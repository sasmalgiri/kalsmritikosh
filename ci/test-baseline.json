{
  "minimumExecutedTests": 882,
  "baselineCommit": "1720bae",
  "note": "Floor, not an equality requirement, measured by the SAME metric the verifier reads: xcresulttool's test-results summary `totalTestCount` (a parameterized test FUNCTION counts as 1; env-gated owner-archive tests count as skipped but remain in the total). 882 = 875 + 7 new OPS-003A.2 tests (SensitiveScopeRepositoryTests grew from 25 to 32: tests 26-32 cover userConfirmed actor+origin storage, whitespace-only actorID rejection, whitespace-only revokedBy rejection, 7th Claim lineage branch via EB-only ref, File\u2192KO privilege propagation on assign, File revoke clears child KOs when no other coverage, File revoke preserves child KOs with remaining direct assignment; none are parameterized so xcresulttool counts each as 1). New tests raise it; the verifier fails when fewer execute or any test fails. Read from build/TestResults.xcresult \u2014 never from console text."
}