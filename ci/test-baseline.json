{
  "minimumExecutedTests": 2443,
  "baselineCommit": "037ee13",
  "note": "Floor, not an equality requirement, measured by the SAME metric the verifier reads: xcresulttool's test-results summary `totalTestCount` (a parameterized test FUNCTION counts as 1; env-gated owner-archive tests count as skipped but remain in the total). 2443 = 2367 (USF-M2) + 76 (USF-FINAL — Universal Source Foundation Closure = USF-008 Canonical Ingestion Completion Contract + USF-009 Progressive Ingestion / On-Demand Upgrade + USF-010 Integrity / Recovery / Reprocessing, delivered on ONE branch, ONE schema bump, TWO hosted CI runs; the +76 mandated floor; 86 tests authored: ProgressiveUpgradeMigrationTests 13 + CanonicalIngestionCompletionTests 13 + SourceUpgradeJobRepositoryTests 14 + SourceUpgradePlannerTests 10 + SourceVersionByteResolverTests 6 + ProgressiveIngestIntegrationTests 10 + SourceReprocessingTests 8 + USFFINALBoundaryTests 12). SCHEMA v87 → v88 (ALL USF-008/009/010 persistence in the one v88 migration): enrichment_jobs REBUILT into an exact-SourceVersion background-work queue [scope_kind legacySubject|sourceVersion; a sourceVersion job carries a non-null source_version_id pinned by FK to source_versions; closed state pending/running/done/failed/blocked/cancelled/superseded; running-requires-lease CHECK; priority + not_before + lease_token/lease_expires_at columns; producer_id/producer_version; partial UNIQUE idx_enrichment_jobs_active_sv gives ONE active job per (source_version_id,kind,producer_id,producer_version) WHERE scope='sourceVersion', and the legacy (subject_id,kind) UNIQUE is preserved for legacySubject rows] + append-only enrichment_job_events [closed action vocabulary, UNIQUE(job_id,sequence), CASCADE on job delete]; legacy rows preserved verbatim as legacySubject with a NULL source_version_id (never guessed); the self-heal sentinel gains v88 column/table markers; a job describes WORK, never readiness. USF-008 (Ingestion/Completion/): IngestionCompletionSnapshot + pure IngestionCompletionEvaluator + IngestionCompletionService reconstruct one canonical completion per EXACT SourceVersion DERIVED ONLY from the durable readiness snapshot (SourceReadinessSnapshot/SourceCompletionState reused verbatim — no second completion vocabulary, no percentage) overlaid with the container-inspection status and the pending/running/failed upgrade kinds; ingest-attempt status can never override it. USF-009 (Ingestion/Upgrade/): SourceProcessingIntent {initialFast|fullAvailable|evidenceUpgrade|analyticalUpgrade} makes the *app* faster — the initial pass takes custody + searchable text + chunks + FTS + readiness and RETURNS searchable (never blocking on evidence/analytical), while an on-demand SourceUpgradeCoordinator plans the MINIMAL missing kinds (SourceUpgradePlanner reuses already-satisfied work — an already-evidence-ready goal schedules nothing), leases them on the exact SourceVersion (SourceUpgradeJobRepository claim/succeed/fail-bounded-retry/failTerminal/block/cancel/supersede/recoverExpiredLeases, priority-ordered), reopens the EXACT bytes (SourceVersionByteResolver — managed reopens the hash-verified vault blob; referenced re-hashes the current file and refuses a changed source with sourceBytesChanged so a mutated file can never mutate the old version), parses via the ONE UniversalParserExecutor at intent .evidenceStructure, and marks a job done ONLY when its readiness postcondition holds (else failTerminal — never silently done); background drains yield to interactive queries via QueryPriorityGate. USF-010 (Ingestion/Upgrade/SourceReprocessingCoordinator): staleness is detected per exact SourceVersion by comparing a parser-dependent dimension's producer_version to the current parser version; reprocess() re-verifies the exact bytes first (throws for changed/missing bytes), then invalidates ONLY the stale parser dimensions (structural/metadata/OCR) via the ready→running invalidate transition and re-stamps them to the prior state with the new producer version — preserving custody (the source_versions row is untouched), search readiness (loader-produced dimensions untouched) and unrelated accepted work; idempotent (a fresh or converged source is .upToDate). Production ingest now records ingest_file_attempts status `.passCompleted` (operational — the pass ran) rather than the legacy `.queryable`; completion still reflects readiness, not the attempt. IngestCoordinator makes ONE plugin decision, splits the readiness advance (search dims default producer; structural/metadata/ocr stamped producerID 'usf-m3.structural' + the plugin version), attaches the completion snapshot, schedules only the missing search/evidence background work, and exposes ensureUpgrade/drainUpgrades/completion/reprocess; AppState constructs SourceUpgradeJobRepository, configures the upgrades, and recovers leases stranded by a prior crash/quit at boot. Non-goals held: NO second source/version/readiness/completion authority, NO Claim promotion, NO AEE/MMI/TBJ/concrete-method work, NO OCR/transcription engine activation, NO network/LLM, NO UI redesign. Migration-matrix group floor rose 201 → 214 (ProgressiveUpgradeMigrationTests, 13). Two-run acceptance protocol: baselineCommit advances from the USF-M2 baseline 4b28324 to the USF-FINAL implementation SHA 037ee13 (which corrects the v88 running-requires-lease CHECK to sourceVersion scope only, preserving the legacy enrichment-job claim contract — the fix for the first PR-head run's single regression) in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2367 = 2311 (USF-M1) + 56 (USF-M2 Safe Container Expansion + Case Coverage Manifest — the +56 mandated floor; 93 tests authored: ContainerManifestMigrationTests 14 + CompoundContainerDetectionTests 11 + ContainerSafetyTests 21 + ContainerInspectionRepositoryTests 8 + StableMemberIntakeTests 4 + ContainerIngestIntegrationTests 15 + CaseCoverageManifestTests 10 + USFM2BoundaryTests 10). USF-M2 combines USF-006 (safe container handling) + USF-007 (case coverage manifest). SCHEMA v87 (two PROCESSING-PROJECTION tables, NOT source/evidence/readiness authorities): container_manifests [one current inspection manifest per exact container SourceVersion; closed status complete/partial/blocked/unsupported/failed; count-consistency CHECK admitted+blocked+unsupported+failed <= regular_file_entries] + container_members [every member disposition incl. never-admitted; closed disposition vocabulary blockedUnsafePath/blockedDepth/blockedEntryLimit/blockedSizeLimit/blockedCompressionRatio/blockedRootBudget/blockedCycle/encrypted/unsupported/failedExtraction/directory/admitted; admitted requires child+hash pinned to source_versions(id,content_hash); non-admitted forbids a fabricated child; UNIQUE(parent,ordinal), duplicate paths allowed]; self-heal sentinel gains the v87 markers; migration-matrix group floor 187 -> 201 (ContainerManifestMigrationTests, 14). UPSTREAM FIX (no USF-M1 reopen): compound-container type detection — DOCX/XLSX/PPTX/ODT/ODS/EPUB are ZIPs, so the ONE intake detector now disambiguates the logical subtype via declared extension (basis stays magicBytes) instead of mis-detecting a real DOCX as .zip; added RAR4/RAR5/7z signatures. New subsystems Ingestion/Container/ + Ingestion/Coverage/. Container handling: ONE immutable ContainerSafetyPolicy (per-container entry/byte/member caps + root-recursion ceilings: depth 8, root member/byte budgets, nested-container count, compression ratio) recorded via policy_version; ZIPContainerExtractor STREAMS each member to a temp file with a running per-member byte cap (a 1 GiB member is never held whole; STORED windowed, DEFLATE chunk-inflated) + correct path-traversal handling (reject only real escapes; a filename containing '..' is safe); ZIPContainerInspector enumerates + classifies every member into exactly one disposition (none silently dropped); ContainerProcessingCoordinator threads a SHARED root budget (not reset per nested container) + ancestor-hash cycle detection + depth ceiling, hands each admitted member to the FULL pipeline via IngestCoordinator (managed custody + a stable kalsmritikosh-container:// origin so a temp extraction path never becomes durable identity; universal parser, never a bypass), records the manifest + every disposition, and recurses into nested ZIP children within the shared limits; RAR/7z stay honest-unsupported (custody kept, contents not enumerated, unknown != empty; no shell/Homebrew/bundled decoder). ContainerInspectionRepository is the SOLE v87 writer (one savepoint: verify container SourceVersion + compatibility, CAS revision, atomic manifest+member replace, derived counts, admitted-child invariants). Case Coverage (USF-007): CaseCoverageManifest is a DETERMINISTIC SNAPSHOT reconstructed from existing authorities (workspace_sources / files+alias_of / source_versions / source_version_relations / source_readiness_* / container_manifests+members) — no second membership or readiness system, no writes, no evidence copies, no overall percentage; the coverage closure covers archiveMember/attachment/message/embedded descendants automatically (derivedConversion excluded), a source reached by several paths is counted once with its occurrence paths preserved, unadmitted/not-inspected members stay visible, readiness reuses SourceReadinessSnapshot/SourceCompletionState verbatim, and two identical DB states yield an identical manifest apart from generatedAt. Non-goals held: no USF-008+/USF-010, no nested RAR/7z decoders, no network/LLM, no Claim/method mutation, no UI redesign. Two-run acceptance protocol: baselineCommit advances from the USF-M1 baseline 7077ea1 to the USF-M2 implementation SHA 4b28324 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2311 = 2263 (USF-002.1) + 48 (USF-M1 Universal Parser Platform — the +48 mandated floor; 50 tests authored: UniversalParserRegistryTests +1 -> 14 [manifest-from-registry] + UniversalContentSurfaceTests 13 + UniversalParserIntegrationTests 14 + USFM1ParserPlatformBoundaryTests 9). USF-M1 combines USF-003 + USF-004 + USF-005 and introduces NO schema change (stays v86; migration-matrix group floor UNCHANGED at 187). It unifies the two previously independent routing authorities — StructuralParserRegistry (SourceType->StructuralParser) and LoaderRegistry (SourceType->Ingestor + TextLoader fallback) — into ONE immutable UniversalParserRegistry where exactly one UniversalParserPlugin owns each SourceType (duplicate id / duplicate type-ownership / blank identity / loader+structural type mismatch all FAIL at construction; never last-wins). Plugins consume the USF-001.2 immutable processing snapshot (never the mutable original); the UniversalParserExecutor fail-closes each result against the request identity (logicalSourceID / sourceVersionID / sourceType / contentHash) AND independently against the ParsedDocument's own identity (the exact-byte parser-hash == version-hash gate now lives here as contentHashMismatch; a structure-less plugin that smuggles a document throws unexpectedStructuralOutput). USF-004 content surfaces (8 closed kinds: text/metadata/structure/tables/images/attachments/transcript/typedFields; coverage complete/partial/notApplicable ONLY — NOT a second readiness system; typedFields stays notApplicable until an accepted producer) project over the EXISTING EvidenceBlock model via ContentSurfaceProjector. ExistingParserPluginAdapter wraps each existing Ingestor (+ optional StructuralParser) WITHOUT rewriting algorithms, reads the snapshot ONCE, runs the loader ONCE via ingestMany, and runs the structural parser over the same bytes. Media (audio/video) stays PreservedOnlyPlugin(deferred) — no AudioLoader/VideoLoader/SpeechTranscriber activated; archives declare container mode; .unknown routes to an explicit system.generic-text-fallback (honest — no fabricated text/blocks for binary). IngestCoordinator makes ONE SourceType->plugin decision and the loader executes ONCE (via the adapter's ingestMany, never both ingest + ingestMany); it no longer consults LoaderRegistry / StructuralParserRegistry (proven by USFM1ParserPlatformBoundaryTests, which allow documentation mentions but forbid CALLS). Durable readiness STILL derives only from committed proof (StructuralPersistenceReceipt + ftsCoverage); no plugin declares isEvidenceReady/isSearchReady/isAnalyticallyReady. ParserCapabilityManifest.generate(registry: UniversalParserRegistry) derives the coverage matrix from the ONE registry. Non-goals held: no v87, no USF-006 (nested archive recursion / RAR / 7z), no OCR/transcription engines, no typed-field producer, no Claim promotion, no network/LLM, no UI redesign. Two-run acceptance protocol: baselineCommit advances from the USF-002.1 baseline 49fa33c to the USF-M1 implementation SHA 7077ea1 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2263 = 2231 (USF-002) + 32 (USF-002.1 Durable Readiness Proof and Exact-Version Coverage: ExactVersionChunkMigrationTests 9 + DurableReadinessProofTests 15 + DurableReadinessIngestTests 8). USF-002.1 adds schema v86 (chunks.source_version_id — a retrieval projection field, soft column + index, with a PROVABLE evidence-block backfill; legacy unprovable chunks stay NULL), a committed StructuralPersistenceReceipt returned by EvidenceStore.persist (readiness advances structure/metadata/OCR ONLY from it — a persistence failure throws → structure FAILED, never ready on in-memory blocks), structural readiness gated on extractionStatus==complete AND every substantive block located (a partial parse or a missing locator stays partial), exact per-version FTS coverage (SourceReadinessRepository.ftsCoverage reconstructed from persisted chunks — a parent's indexing readiness never counts a child attachment's chunks), and repository hardening (positive structural/index readiness requires a durable basis; indexing coverage units must equal the DB-derived coverage; soft ftsIndex/vectorIndex/custody bases must name THIS exact version; empty plans rejected; invalidation requires a producer-version or basis change, not just a reason). ParseCoverageReport.from(_ doc:) is now INTERNAL (non-authoritative); the durable path is SourceReadinessRepository → SourceReadinessSnapshot → ParseCoverageReport/SourceReadinessSummary. Migration-matrix group floor rose 178→187 (ExactVersionChunkMigrationTests, 9). Two-run acceptance protocol: baselineCommit advances from the USF-002 baseline 0ac08d7 to the USF-002.1 implementation SHA 49fa33c in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (no green claim here; the PR-head run id + counts are recorded in the PR after the hosted run). PRIOR: 2231 = 2159 (USF-001.2) + 72 (USF-002 Independent Source Readiness Dimensions: SourceReadinessMigrationTests 17 + SourceReadinessEvaluatorTests 16 + SourceReadinessRepositoryTests 18 + SourceReadinessIngestIntegrationTests 12 + USF002ReadinessBoundaryTests 9). USF-002 adds schema v85 (source_readiness_aggregates + source_readiness_dimensions [ten independent closed dimensions per EXACT source version: preservation/metadata/text/structural/ocr/transcription/indexing/basicQA/typedFields/analytical] + append-only source_readiness_events; conservative v84→v85 backfill from canonical evidence; self-heal column probe), the deterministic SourceReadinessEvaluator (search ≠ evidence ≠ analytical; completion state DERIVED, never stored/caller-set; no overall percentage), the atomic SourceReadinessRepository (one savepoint: existence + optimistic CAS + validation + dimension writes + one aggregate revision bump + contiguous events + snapshot rebuild; closed transition rules; invalidation), intake bootstrapping the ten dimensions in the SAME transaction that creates a new source version (unchanged/moved/aliased reuse the canonical readiness — no second aggregate), pipeline advancement of text/indexing/structure from durable representations, and SourceReadinessSummary/ParseCoverageReport refactored to consume the durable authority (live IngestProgress stays separate). No second source/version authority; readiness never confirms a Claim. Migration-matrix group floor rose 161→178 (SourceReadinessMigrationTests, 17). Non-goals (deferred): parser-registry work (USF-003+), OCR/transcription engines, typed-field extraction algorithms, reprocessing UX (USF-010), major Sources UI redesign, network/LLM, Claim promotion. Two-run acceptance protocol: baselineCommit advances from the USF-001.2 baseline 624e21b to the USF-002 implementation SHA 0ac08d7 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2159 = 2135 (USF-001.1) + 24 (USF-001.2 Exact-Byte Binding and Alias Transition Closure: ExactByteBindingMigrationTests 9 + ExactByteBindingIntakeTests 8 + AliasTransitionClosureTests 7). USF-001.2 adds schema v84 (source_versions rebuilt so a non-legacy custody hash must be HEXADECIMAL — `content_hash NOT GLOB '*[^0-9a-f]*'` closes the v83 gap that accepted any 64-char lowercase string; the migration demotes existing non-hex custody rows to legacyImported; self-heal gains the v84 GLOB probe), an immutable per-intake processing snapshot (SourceByteCapture.captureToSnapshot streams the exact bytes into a temp snapshot in the SAME verified pass as the hash; the loader and structural parser consume ONLY the snapshot; the parser's own hash therefore equals the source version's hash — the USF-001.1 foreign-hash substitution is REMOVED and a mismatch writes no structural artifacts; the coordinator removes the snapshot after processing and rebinds KO identity to the original url), alias transition closure (an alias URL whose bytes change is either re-pointed at another canonical reusing its version, or promoted in place to its own canonical source — never a second file row for the URL), and managed known-hash vault reuse (a managed version whose hash is already vaulted reuses that verified address; managedCopyStored is derived from an actual present address, never from the absence of a copy failure). Migration-matrix group floor rose 152→161 (ExactByteBindingMigrationTests, 9). baselineCommit = 624e21b (USF-001.2 hosted-green implementation SHA, CI run 30678478955, all 6 jobs green: build-and-test 2159 executed / 2157 passed / 2 skipped / 0 failed (floor 2159); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the USF-001.1 baseline 181bf0f to 624e21b. PRIOR: 2135 = 2103 (USF-001) + 32 (USF-001.1 Single-Path Intake and Custody Integrity: SinglePathIntakeIntegrityMigrationTests 11 + USF001SinglePathIntegrityTests 16 + 5 new IngestCoordinatorSafeIntakeTests terminal-attempt tests). USF-001.1 adds schema v83 (source_versions non-legacy SHA-256 + same-logical supersedes composite FK + UNIQUE(id,logical_source_id)/(id,content_hash); source_intake_receipts composite FKs pinning the receipt to the version's logical source AND hash; ingest_file_attempts both-or-neither + composite FK), makes the intake coordinator MANDATORY (legacy ingest branch removed — intake is the sole pre-parser identity authority), makes EvidenceStore ATTACH-ONLY in one savepoint (fail-closed on version/logical/hash/document-id mismatch; never creates or retires a version), single-pass verified vault storage (bytes hashed while copied → address always matches content), ONE authoritative type detector (path pattern → magic → declared extension → unknown; IngestCoordinator uses handle.detectedType), fully atomic in-savepoint identity resolution + existing-alias re-intake, and ONE version-linked terminal attempt per ingest. Migration-matrix group floor rose 141→152 (SinglePathIntakeIntegrityMigrationTests, 11). baselineCommit = 181bf0f (USF-001.1 hosted-green implementation SHA, CI run 30644290285, all 6 jobs green: build-and-test 2135 executed / 2133 passed / 2 skipped / 0 failed (floor 2135); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the USF-001 baseline a176362 to 181bf0f. PRIOR: 2103 = 2047 (PM-004.1) + 56 (USF-001 Universal Safe Intake: UniversalSourceIntakeMigrationTests 14 + CanonicalSourceIntakeRepositoryTests 20 + IngestCoordinatorSafeIntakeTests 13 + USF001UniversalSafeIntakeBoundaryTests 9). USF-001 adds schema v82 (source_versions rebuilt with intake custody metadata — filename/detected type/detection_basis/size/custody_mode/preservation_status/vault_address/intake_recorded_at, NOT-NULL with DEFAULTs so legacy short-form inserts still round-trip; content_hash left unconstrained as pre-v82; one-current partial UNIQUE index; source_intake_receipts + source_version_relations + version-linked ingest_file_attempts), the intake subsystem (streaming SHA-256 capture, CanonicalSourceIntakeRepository atomic identity transaction, UniversalSourceIntakeCoordinator, EvidenceVault.storeStreaming), and the IngestCoordinator pre-parser rewire (custody before loader; unchanged/moved/aliased skip parsing; loader/parser failure keeps custody; EvidenceStore attaches the parsed document to the intake-created version). No second source/version authority; source_versions remains the one version authority. Migration-matrix group floor rose 127→141 (UniversalSourceIntakeMigrationTests, 14). baselineCommit = a176362 (USF-001 hosted-green implementation SHA, CI run 30637463242, all 6 jobs green: build-and-test 2103 executed / 2101 passed / 2 skipped / 0 failed (floor 2103); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the PM-004.1 baseline 27c7ced to a176362. PRIOR: 2047 = 2030 (PM-004) + 17 (PM-004.1 Lifecycle Ledger and Migration Hardening: MethodLifecycleLedgerHardeningMigrationTests 9 + MethodLifecycleEpochEnforcementTests 8). PM-004.1 adds schema v81 (rebuilds method_runs for the REVERSE supersession CHECK, method_reviews for non-blank review_key + non-negative reviewed_content_revision + not-both-node-and-finding CHECKs, method_validation_results for non-blank validation_batch_id + non-negative evaluated_content_revision CHECKs — NO new column), makes the migration pass atomic (a single OUTER savepoint wraps every pending migration AND the final PRAGMA foreign_key_check, so a post-pass FK violation rolls the whole pass + user_version back), and makes applyLifecyclePlan self-enforce gate epochs (a review must evaluate the effective content revision — current, or current+1 on reopen; a validation batch must be one non-legacy batch, every row owned by the run and evaluating the current revision). The self-heal sentinel gains v81 CHECK-text probes (v81 adds no column, so the column probe cannot distinguish v80). Migration-matrix group floor rose 118→127 (MethodLifecycleLedgerHardeningMigrationTests, 9). baselineCommit = 27c7ced (PM-004.1 hosted-green implementation SHA, CI run 30628380062, all 6 jobs green: build-and-test 2047 executed / 2045 passed / 2 skipped / 0 failed (floor 2047); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the PM-004 baseline 369a4b3 to 27c7ced. PRIOR: 2030 = 1950 (PM-003) + 80 (PM-004 Generic Method Lifecycle, Validation, Review and Durable Reopen: MethodLifecycleStateMachineTests 15 + ProfessionalMethodValidationTests 12 + MethodReviewGateTests 12 + ProfessionalMethodLifecycleEngineTests 19 + PM004MethodLifecycleBoundaryTests 9 + MethodLifecycleMigrationTests 13). Prior-unit PM-001/002/003 tests the v80 bump touched were updated in place (no count change). PM-004 adds schema v80 (method_runs rebuilt for content_revision + 'paused'; method_evidence_links.input_role; method_reviews.review_key + reviewed_content_revision; method_validation_results.validation_batch_id + evaluated_content_revision; new method_run_events durable audit table) + the closed lifecycle state machine, deterministic validator runtime, definition-keyed human-only review gates, completion gates, human-authorized durable reopen (a distinct contentRevision epoch invalidates prior validation/review), and terminal immutability. Content mutations are gated to draft/active and bump content_revision via the same optimistic-revision CAS. The migration disables foreign-key enforcement for the whole pass (SQLite-recommended for a parent-table rebuild) + runs PRAGMA foreign_key_check. No concrete method (Five Whys/Fishbone/CAPA/…), UI, LLM, network or Claim promotion. Migration-matrix group floor rose 105→118 (MethodLifecycleMigrationTests, 13). baselineCommit = 369a4b3 (PM-004 hosted-green implementation SHA, CI run 30624772397, all 6 jobs green: build-and-test 2030 executed / 2028 passed / 2 skipped / 0 failed (floor 2030); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the PM-003 baseline 68bd22a to 369a4b3. PRIOR: 1950 = 1905 (PM-002) + 45 (PM-003 Professional-Method Registry + Stage-3 Adapter Bridge: ProfessionalMethodRegistryTests 16 + ProfessionalMethodWorkflowBridgeTests 10 + RegisteredMethodStepExecutorTests 10 + PM003MethodRegistryBridgeBoundaryTests 9). PM-003 adds NO schema change (stays v79) and NO lifecycle service (PM-004): one code-backed ProfessionalMethodRegistry (wraps the generic backbone; no definition table), reference-only bridge envelopes + ProfessionalMethodWorkflowBridge, and the v2 RegisteredMethodStepExecutor (same id, version 2, schema-2-bound; v1 + schema-1 untouched). Migration-matrix group floor stays 105 (PM-003 has no migration). PRIOR: 1905 = 1866 (PM-001) + 39 (PM-002 Method Persistence: MethodRunMigrationTests 15 + MethodRunRepositoryTests 15 + PM002MethodPersistenceBoundaryTests 9; the +4 over the first PM-002 pass are the owner-review corrections: evidence-link stable id, single-snapshot aggregate, mandatory non-run validation subject + its schema CHECK). PM-002 adds schema v79 (the 8-table MethodRun aggregate ledger) + the authoritative MethodRunRepository under Kalsmritikosh/Method/Persistence/. Stage-4 persistence only: no concrete method, lifecycle policy, Stage-3 bridge, UI, LLM, network or Claim promotion. The migration-matrix group floor also rose 90→105 (MethodRunMigrationTests, 15). Tests are cumulative. New tests raise it; the verifier fails when fewer execute or any test fails. Read from build/TestResults.xcresult — never from console text. baselineCommit = 68bd22a (PM-003 hosted-green implementation SHA, CI run 30608063827, all 6 jobs green: build-and-test 1950 executed / 1948 passed / 2 skipped / 0 failed; migration-matrix 105/14; sensitive-export 122/7; parser-fixtures 78/20; report-receipt-integrity 77/7; architecture-guards clean). PM-003 introduces NO concrete method and NO lifecycle service (PM-004). Prior PM-002 baseline 28b22dd (run 30605062552). Stage 3 (Persona Job Engine, PJE-001..012) COMPLETE — see STAGE3_ACCEPTANCE.md."
}
