{
  "minimumExecutedTests": 938,
  "baselineCommit": "HEAD",
  "note": "Floor, not an equality requirement, measured by the SAME metric the verifier reads: xcresulttool's test-results summary `totalTestCount` (a parameterized test FUNCTION counts as 1; env-gated owner-archive tests count as skipped but remain in the total). 938 = 935 - 1 removed (noAccessContextBypassesScopeFilter) + 4 added OPS-003C.2 (exportWithWrongPurposeDenied, exportWithMismatchedWorkspaceIDDenied, blockedClaimProducesEmptyManifestAndHashes, privilegedTextAbsentFromAllSections). New tests raise it; the verifier fails when fewer execute or any test fails. Read from build/TestResults.xcresult — never from console text."
}
