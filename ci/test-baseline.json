{
  "minimumExecutedTests": 945,
  "baselineCommit": "HEAD",
  "note": "Floor, not an equality requirement, measured by the SAME metric the verifier reads: xcresulttool's test-results summary `totalTestCount` (a parameterized test FUNCTION counts as 1; env-gated owner-archive tests count as skipped but remain in the total). 945 = 926 + 19 new OPS-003C tests (WorkProductScopeEnforcementTests: 5 — scopedAccessDeniedWhenRepoNotWired, privilegedEvidenceKOBlockedFromExport, nonPrivilegedEvidenceKOPermittedForExport, sensitivityCeilingBlocksRestrictedClaim, noAccessContextBypassesScopeFilter; plus 14 additional tests from other suites that ran above the prior floor). New tests raise it; the verifier fails when fewer execute or any test fails. Read from build/TestResults.xcresult — never from console text."
}
