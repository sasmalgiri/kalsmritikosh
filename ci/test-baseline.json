{
  "minimumExecutedTests": 967,
  "baselineCommit": "80c07d72561665e510d700336621b0d36f568bee",
  "note": "Floor, not an equality requirement, measured by the SAME metric the verifier reads: xcresulttool's test-results summary `totalTestCount` (a parameterized test FUNCTION counts as 1; env-gated owner-archive tests count as skipped but remain in the total). 967 = 957 (OPS-003D.1.2.1 CI floor, run 30261625706) + 10 (CI-001C SensitiveScopeEndToEndTests — 10 cross-surface E2E tests). Tests are cumulative — security tests are never exchanged. New tests raise it; the verifier fails when fewer execute or any test fails. Read from build/TestResults.xcresult — never from console text. baselineCommit will be updated to the CI-001C hosted-green SHA in the evidence commit."
}
