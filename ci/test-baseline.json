{
  "minimumExecutedTests": 3005,
  "baselineCommit": "5e6c942",
  "note": "Floor, not an equality requirement, measured by the SAME metric the verifier reads: xcresulttool's test-results summary `totalTestCount` (a parameterized test FUNCTION counts as 1; env-gated owner-archive tests count as skipped but remain in the total). 3005 = 2986 (LAB-004 hosted actual) + 19 (LAB-005 Data Quality & Lineage — Stage C fifth unit; 19 tests authored, none parameterized: WorkbenchDataQualityEvaluatorTests 10 + WorkbenchDataQualityAnalyzerTests 3 + WorkbenchDataQualityBoundaryTests 6). NO SCHEMA CHANGE (stays v94; migration-matrix group floor UNCHANGED at 305) — LAB-005 is a PURE read-only analysis layer. New subsystem Kalsmritikosh/Workbench/Quality/: generalises a stale-data warning model to the full evidence-quality set — WorkbenchDataQuality closed 14-kind warning vocabulary (), severity {info/caution/blocking} a FIXED property of the kind (never a function of a confidence score), each warning carrying its LAB-004 EvidenceInspectionTarget lineage; WorkbenchQualityInputs bundles externally-collected facts (defaults empty — an undeterminable fact is never guessed). WorkbenchDataQualityEvaluator (PURE, deterministic): computes record-derivable checks (missing value, missing custody hash, duplicate source) from the dataset record, folds in external facts (stale/inaccessible source, low OCR, ambiguous identity, mixed date precision, unsupported transformation, non-independent corroboration, unresolved contradiction, incomplete workspace scope, formula-vs-displayed discrepancy), and — when a LAB-003 scenario projection is supplied — flags its unreviewed overlay values; deterministic ordering; every source-mapped warning carries lineage. WorkbenchDataQualityAnalyzer (actor) COMPOSES the existing dataset + scenario read paths (WorkbenchDatasetRepository.staleBindings for superseded versions, accepted scenario reviews for reviewed overlays) and calls the evaluator — no new SQL, canonical evidence read-only; wired AppState.workbenchDataQuality. Non-goals held: never mutates canonical evidence, never resolves a contradiction, never silently fixes a value; no second authority; no schema; no network/LLM. Regression: full local suite 3023 total / 3021 passed / 2 skipped / 0 failed (no prior-guard changes needed — no schema bump). Two-run acceptance protocol: baselineCommit advances from the LAB-004 baseline 85f2ece to the LAB-005 implementation SHA 5e6c942 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). NEXT: LAB-006 Simple & Advanced modes (closes Stage C). PRIOR: 2986 = 2969 (LAB-003 hosted actual) + 17 (LAB-004 Shared Visual Surfaces + universal one-action evidence inspection — Stage C fourth unit; 17 tests authored, none parameterized: WorkbenchVisualSurfaceTests 6 + WorkbenchSurfaceProjectorTests 5 + WorkbenchVisualBoundaryTests 6). NO SCHEMA CHANGE (stays v94; migration-matrix group floor UNCHANGED at 305) — LAB-004 is a PURE presentation projection, not persistence. New subsystem Kalsmritikosh/Workbench/Visual/: the ONE shared visual-surface model for the DataLab's reusable native canvases (closed 14-kind vocabulary table/timeline/matrix/board/relationshipGraph/transactionFlow/chart/fishbone/fiveWhysChain/mapLocation/documentComparison/evidenceWall/checklistForm/notebookEditor). The load-bearing contract rule — EVERY visual object supports one-action evidence inspection — is enforced structurally: every WorkbenchVisualElement carries a non-optional WorkbenchElementProvenance {source(EvidenceInspectionTarget) / scenario(original additive) / derived(inputs+basis) / userEntered(actor) / none(reason)}, so a source-backed element drills through to the EXACT canonical target + SourceVersion + locator (EvidenceInspectionTarget reuses WorkbenchBindingTargetKind + SourceLocator, forking neither) and every other case resolves to an explicit honest basis — there is no dead / un-inspectable element. WorkbenchSurfaceProjector (pure, no DB, no mutation of canonical evidence or dataset cells): tableSurface(from a WorkbenchDatasetRecord) maps each cell's provenance from its kind + LAB-001 binding (source→target, deterministicCalculation/modelProposal→derived, userEntered/userCorrected/reviewed→authored, unbound source→honest none), column headers structural; scenarioComparisonSurface(from a LAB-003 WorkbenchScenarioProjection) builds a Document-comparison surface preserving the ORIGINAL source lineage (additive, never replaced by scenario lineage); a generic build() REFUSES an element with no provenance (mandatory inspection, fail-closed). No AppState wiring (stateless, computed on demand, like ParseCoverageReport / CaseCoverageManifest). Non-goals held: no actual SwiftUI canvas views yet (product-shell stage), no second data/transformation/scenario/privacy authority, no schema, no network/LLM. Regression: full local suite 3004 total / 3002 passed / 2 skipped / 0 failed (no prior-guard changes needed — no schema bump). Two-run acceptance protocol: baselineCommit advances from the LAB-003 baseline 35c37ca to the LAB-004 implementation SHA 85f2ece in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). NEXT: LAB-005 Data Quality & Lineage. PRIOR: 2969 = 2924 (LAB-002 hosted actual) + 45 (LAB-003 Scenario Branching, Undo, Redo and Reviewed Promotion — Stage C third unit; 45 tests authored, none parameterized: WorkbenchScenarioMigrationTests 14 + WorkbenchScenarioRepositoryTests 21 + WorkbenchScenarioBoundaryTests 9 + WorkbenchScenarioAcceptanceTests 1). SCHEMA v93 → v94 (ONE bump): four NEW tables (workbench_scenarios + workbench_scenario_operations + workbench_scenario_reviews + workbench_scenario_events). A scenario is a NON-DESTRUCTIVE analytical overlay on a LAB-001 dataset: an append-only OPERATION LOG plus an undo/redo pointer (current_op_seq) — the current state is REPLAYED from the live operations up to the pointer (never a stored mutated blob), so history is reconstructable and undo/redo is a deterministic pointer move. A new operation after an undo ABANDONS (retains for audit, never resurrects) the redo branch. Canonical evidence, the source cells and the LAB-002 derivations are READ-ONLY (guard-enforced: the scenario layer performs no INSERT/UPDATE/DELETE of the 8 canonical evidence tables OR of workbench_cells/_fields/_rows/_transformations/_derivations). A transformation over a scenario reuses the ONE WorkbenchTransformEngine over a scenario-projected record (no second engine, no LAB-002 mutation). SensitiveScope is composed from the SHARED authority (never widened, fail-closed). Promotion of scenario content into canonical/professional truth is ONLY an explicit human-reviewed action recorded in workbench_scenario_reviews, routed to an EXISTING authority (userCorrection/workingFinding/methodRunInput/claimReview/workProductInput) with the resulting reference — there is NO makeCanonical() and no direct claim/method_run write; a rejected promotion leaves canonical state unchanged; a row inclusion/exclusion op is not promotable. Discard marks a scenario inactive while preserving its full audit history. Staleness (base dataset revision changed / bound source version superseded) is SURFACED, never silently rebased. Ownership edges are hard FK + CASCADE (dataset→scenarios→operations/reviews/events; operation→reviews). CHECKs: nonblank title/actor/reviewer, closed status/op-kind/target-kind/op-status/destination/decision/event-action vocabularies, cell op requires a field, per-scenario unique operation + event sequence, pointer/base-revision/revision bounds. NEW tables so the self-heal column probe distinguishes v94. Wired AppState.workbenchScenarios. migration-matrix group floor 291 → 305 (WorkbenchScenarioMigrationTests 14). Regression: full local suite 2987 total / 2985 passed / 2 skipped / 0 failed (MigrationMatrixTests pins latest v94; WorkbenchScenarioBoundaryTests / other prior guards use >= for the schema). Two-run acceptance protocol: baselineCommit advances from the LAB-002 baseline d38e308 to the LAB-003 implementation SHA 35c37ca in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). NEXT: LAB-004 Shared Visual Surfaces. PRIOR: 2924 = 2854 (LAB-001 hosted actual) + 70 (LAB-002 Safe Transformation Engine — Stage C second unit; 70 tests authored, none parameterized: WorkbenchTransformMigrationTests 14 + WorkbenchExpressionTests 14 + WorkbenchEvaluatorTests 13 + WorkbenchTransformEngineTests 12 + WorkbenchTransformRepositoryTests 9 + WorkbenchTransformBoundaryTests 8). SCHEMA v92 → v93 (ONE bump): three NEW tables (workbench_transformations + workbench_derivations + workbench_derivation_inputs) + a workbench_dataset_events rebuild that widens the action CHECK with 'transformed' (existing rows copied verbatim). A safe PARSED expression language (tokenizer → recursive-descent parser → tree-walking evaluator) computes every derived value — NO `eval`, no dynamic-code path (guard-enforced: no NSExpression/JavaScriptCore/Process/dlopen), no network, no clock, no locale; an unknown function or stray symbol is a PARSE ERROR, never silently accepted. WorkbenchValue is a total, locale-independent runtime value coerced through the canonical FactSchemaRegistry.ValueShape; a not-computable result (missing operand, divide-by-zero, unparseable date) is the honest null, never a fabricated zero. The closed transform kind vocab {calculatedColumn,runningTotal,filter,sort,deduplicate,aggregate} is computed; pivot/join/rollingCalculation are recognised but returned as an honest `.unsupported` outcome (never silently mis-computed — the LAB-005 unsupported-transformation surface). Per the contract every derived value persists its formula/transformation + the EXACT input cell IDs it read + engine version + output, so it is reproducible (recompute() re-runs a stored spec) and auditable. WorkbenchTransformRepository (actor) computes with the pure engine then persists atomically (one SAVEPOINT): a calculated/running-total column becomes NEW deterministicCalculation cells (status DETERMINISTICALLY_DERIVED — the read-only source cells are never mutated; guard-enforced no INSERT/UPDATE/DELETE of the 8 canonical evidence tables), a filter/sort/deduplicate becomes a recorded projection, an aggregate becomes grouped derivations; revision CAS; an unsupported kind or bad formula is REFUSED with nothing written; a 'transformed' event is appended. Wired AppState.workbenchTransforms. Truth boundaries preserved: derived value ≠ source observation; a transformation never touches canonical evidence; no second dataset/transformation/privacy authority. Non-goals held: no LAB-003 scenarios/undo, no visual surfaces/UI, no network/LLM. migration-matrix group floor 277 → 291 (WorkbenchTransformMigrationTests 14). Regression: full local suite 2942 total / 2940 passed / 2 skipped / 0 failed (prior LAB-001 oneSchemaBump guard tightened to the exact 'CREATE TABLE <t> (' match so the v93 events rebuild is not miscounted; MigrationMatrixTests pins latest v93; WorkbenchBoundaryTests schema pin relaxed to >= 92). Two-run acceptance protocol: baselineCommit advances from the LAB-001 baseline 23c9ad6 to the LAB-002 implementation SHA d38e308 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). NEXT: LAB-003 Scenario & Undo. PRIOR: 2854 = 2817 (PM-010 hosted actual) + 37 (LAB-001 canonical Workbench dataset model — Stage C first unit; 37 tests authored, none parameterized: WorkbenchDatasetMigrationTests 14 + WorkbenchDatasetRepositoryTests 14 + WorkbenchBoundaryTests 9). SCHEMA v91 → v92 (ONE bump): seven NEW tables (workbench_datasets/_fields/_rows/_cells/_source_bindings/_saved_views/_dataset_events) — the ONE canonical DataLab dataset authority, superseding the isolated EvidenceDataset prototype (retained decode-only). migration-matrix group floor 263 → 277 (WorkbenchDatasetMigrationTests 14). A Workbench dataset is DERIVED from and REFERENCES the read-only evidence ledger — never a second truth store: every cell declares a closed provenance kind {sourceValue,deterministicCalculation,userEntered,userCorrected,modelProposal,reviewed}; a source cell drills through a WorkbenchSourceBinding to its exact canonical origin (block/claim/event/… + SourceVersion + locator); status reuses the canonical EvidenceStatus and shapes reuse FactSchemaRegistry.ValueShape (no forked vocabulary). WorkbenchDatasetRepository (actor, SAVEPOINT + revision CAS + append-only events, deterministic-order reads, fetch() reopen) NEVER mutates canonical evidence (read-only isolation, guard-enforced); Gate-4 drill-through validates the binding target exists + SourceVersion consistency; Gate-6 stale-source detection reports superseded versions without erasing the dataset. SensitiveScope propagation (WorkbenchSensitivity) composes the SHARED authority (fail-closed, no forked privacy). Legacy conversion delegates into Workbench* (status→kind map + sourceBlockIDs→bindings, `converted` event); EvidenceDataset marked SUPERSEDED. Wired AppState.workbenchDatasets. Truth boundaries: row≠evidence, cell≠confirmed Claim, derived≠observed, user-entered≠source evidence, scenario≠canonical. Non-goals held: no LAB-002 transformation engine yet, no scenarios/visual/UI, no second dataset/transformation/privacy authority, no network/LLM. Regression: full local suite 2872 / 2870 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the PM-010 baseline b77f405 to the LAB-001 implementation SHA 23c9ad6 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). NEXT: LAB-002 Safe Transformation Engine. PRIOR: 2817 = 2811 (PM-009 hosted actual) + 6 (PM-010 Risk Matrix + Decision Matrix — Stage B FINAL unit, completing the professional method catalog MET-01..16; 6 tests authored, none parameterized: PM010ConcreteMethodTests 6). NO SCHEMA CHANGE (stays v91; migration-matrix group floor UNCHANGED at 263). MET-15 + MET-16 on the PM-001..004 foundation, registered in ProfessionalMethodCatalog.standardMethods (now 16 concrete methods — the full catalog). Risk Matrix: rated-item nodes each citing the basis for likelihood/impact; NO finding so a rating is never a certain outcome; confirmRatings human review. Decision Matrix: option nodes whose scores cite their basis; a selectedOption finding is admitted ONLY as the outcome of a recorded HUMAN decisionRecorded review TARGETING that finding at the current content revision (same gate as Root-Cause's confirmedRootCause) — the app never makes the final decision. STAGE B (Concrete Professional Methods) COMPLETE: MET-01 Brainstorming, MET-02 5W1H, MET-03 Hypothesis Matrix, MET-04 Evidence Collection Plan, MET-05 Five Whys, MET-06 Fishbone, MET-07 Root-Cause, MET-08 CAPA, MET-09 Effectiveness Review, MET-10 Contradiction Matrix, MET-11 Gap Analysis, MET-12 Timeline Analysis, MET-13 Relationship Analysis, MET-14 Transaction Flow, MET-15 Risk Matrix, MET-16 Decision Matrix — all as code-backed ProfessionalMethodDefinition + deterministic validators on the ONE ProfessionalMethodLifecycleEngine, no new run store / lifecycle / registry / schema, all persona-neutral, all human-gated where the catalog requires. Non-goals held: no Stage C (Evidence Workbench/DataLab) yet, no shell/persona/collaboration, no schema, no network/LLM. Regression: full local suite 2835 / 2833 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the PM-009 baseline 4da90cf to the PM-010 implementation SHA b77f405 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). NEXT: Stage C — Evidence Workbench / DataLab (LAB-001..006). PRIOR: 2811 = 2800 (PM-008 hosted actual) + 11 (PM-009 analytical method pack — Stage B fifth unit; 11 tests authored, none parameterized: PM009ConcreteMethodTests 11). NO SCHEMA CHANGE (stays v91; migration-matrix group floor UNCHANGED at 263). MET-10..MET-14 on the PM-001..004 foundation, registered in ProfessionalMethodCatalog.standardMethods (now 14 concrete methods). All five are category .analysis with an EMPTY output contract (no findings) so none asserts a conclusion: Contradiction Matrix (every conflict preserves BOTH sides, >=2 evidence links, no winner), Gap Analysis (each gap states a reason, searchedScope anchor, absence never implies wrongdoing), Timeline Analysis (each dated row cites its source OR is marked undated, no invented/padded dates), Relationship Analysis (each asserted edge cites evidence), Transaction Flow (each transaction's amount traces to a source, no fraud conclusion). Each confirms inclusion via a required human review. Non-goals held: no PM-010 yet, no DataLab/shell/persona/collaboration, no schema, no network/LLM. Regression: full local suite 2829 / 2827 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the PM-008 baseline 763ec07 to the PM-009 implementation SHA 4da90cf in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). PRIOR: 2800 = 2791 (PM-007 hosted actual) + 9 (PM-008 Root-Cause Assessment + CAPA + Effectiveness Review — Stage B fourth unit; 9 tests authored, none parameterized: PM008ConcreteMethodTests 9). NO SCHEMA CHANGE (stays v91; migration-matrix group floor UNCHANGED at 263). MET-07 + MET-08 + MET-09 on the PM-001..004 foundation, registered in ProfessionalMethodCatalog.standardMethods (now 9 concrete methods). Root-Cause Assessment: candidate causes each carrying an evidence profile; it is the FIRST method whose output contract admits a finding (confirmedRootCause), and RootCauseValidator refuses completion unless every such finding is backed by a HUMAN rootCauseDecision review TARGETING that exact finding at the current content revision — candidate ≠ confirmed, the app never confirms a root cause itself. CAPA: corrective/preventive action nodes that MUST each link the cause they address; closure is a human review (capaClosure); no effectiveness finding (action completion ≠ effectiveness); execution uses the existing ProfessionalTask path. Effectiveness Review: evidence-backed check nodes (a check with no supporting evidence blocks — no declaring effectiveness without evidence); the effective/partial/ineffective outcome is an independent human review (effectivenessDecision), never inferred from CAPA closure; a content-revision bump invalidates a stale decision. Non-goals held: no PM-009..010 yet, no DataLab/shell/persona/collaboration, no schema, no network/LLM. Regression: full local suite 2818 / 2816 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the PM-007 baseline 095acdb to the PM-008 implementation SHA 763ec07 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). PRIOR: 2791 = 2783 (PM-006 hosted actual) + 8 (PM-007 Five Whys + Fishbone/Ishikawa — Stage B third unit; 8 tests authored, none parameterized: PM007ConcreteMethodTests 8). NO SCHEMA CHANGE (stays v91; migration-matrix group floor UNCHANGED at 263). MET-05 Five Whys + MET-06 Fishbone on the PM-001..004 foundation, registered in ProfessionalMethodCatalog.standardMethods (now 6 concrete methods). Five Whys: each why is a node; a deeper level is a child via parentNodeID; FiveWhysValidator lets the chain deepen ONLY from a supported level (a supporting evidence link OR an explicit MethodAssumption) and blocks deepening from an unsupported why — no forcing five levels; a leaf unsupported why is the honest stop; NO findings so the method never records a confirmed root cause (that is MET-07); confirmLevels human review. Fishbone/Ishikawa: branch + candidateCause nodes (a candidate cause is a child of its branch via parentNodeID); completion needs at least one branch populated with a candidate cause; NO findings so no bone is ever the confirmed root cause; confirmBranches human review. Both anchored to the problem via a problemStatement input-role evidence link. Non-goals held: no PM-008..010 yet, no DataLab/shell/persona/collaboration, no schema, no network/LLM. Regression: full local suite 2809 / 2807 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the PM-006 baseline 8726be0 to the PM-007 implementation SHA 095acdb in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). PRIOR: 2783 = 2776 (PM-005 hosted actual) + 7 (PM-006 Hypothesis Matrix + Evidence Collection Plan — Stage B second unit; 7 tests authored, none parameterized: PM006ConcreteMethodTests 7). NO SCHEMA CHANGE (stays v91; migration-matrix group floor UNCHANGED at 263). MET-03 Hypothesis Matrix + MET-04 Evidence Collection Plan implemented as ProfessionalMethodDefinition + deterministic validators on the PM-001..004 foundation, registered in ProfessionalMethodCatalog.standardMethods (now 4 concrete methods). Hypothesis Matrix: each hypothesis is a node; its evidence profile is its evidence links typed for/against/neutral (supporting/contradicting/contextual); HypothesisMatrixValidator requires EVERY hypothesis to carry an evidence profile; the output contract allows NO findings so the app never records a winning hypothesis (no auto-winner) — a hypothesis becomes a Claim only via the reviewed workflow; confirmHypotheses human review. Evidence Collection Plan: each node is a request for evidence NOT yet in hand; EvidenceCollectionPlanValidator requires each request to state its need and BLOCKS a request that cites the sought evidence as already existing (a per-request supporting link) — no asserting the requested evidence exists; output contract allows no findings; confirmRequests human review; anchored to case context. Non-goals held: no PM-007..010 yet, no DataLab/shell/persona/collaboration, no schema, no network/LLM. Regression: full local suite 2801 / 2799 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the PM-005 baseline 799b971 to the PM-006 implementation SHA 8726be0 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (recorded in the PR after the hosted run — no green claim; floor corrected to the hosted totalTestCount if the metric differs). PRIOR: 2776 = 2762 (TBJ-FINAL hosted actual) + 14 (PM-005 Brainstorming + 5W1H — Stage B first unit; 14 tests authored, none parameterized: PM005ConcreteMethodTests 8 + PM005MethodCatalogBoundaryTests 6). NO SCHEMA CHANGE (stays v91; migration-matrix group floor UNCHANGED at 263) — concrete methods are code-backed definitions + validators on the existing PM-001..004 foundation, not persistence. Two concrete professional methods (MET-01 Brainstorming, MET-02 5W1H) implemented as a ProfessionalMethodDefinition + deterministic ProfessionalMethodValidating validators, assembled by the ONE new Method/Catalog/ProfessionalMethodCatalog into the method + validator registries the generic ProfessionalMethodLifecycleEngine already consumes — NO second run store / lifecycle / registry / validation / Claim-promotion authority. Brainstorming: typed proposal items (idea/question/assumption/hypothesis/lead/known-Claim/evidence-need/possible-cause); its output contract allows NO finding kinds so the conformance gate rejects any fact promotion, and BrainstormingValidator blocks an item flipped to human-accepted-for-workflow inside the method (promotion happens only via the reviewed workflow); anchored to the case context by a required caseContext evidence link (satisfying the foundation's completed-method-connects-to-evidence invariant without upgrading a proposal). 5W1H: six slot nodes (who/what/when/where/why/how); FiveW1HValidator blocks a slot that carries an answer but cites no supporting evidence and is not marked unknown (no fabricated answers for unknown slots); each answered slot confirmed through the required HUMAN review confirmSlots. Both drive the generic engine create→start→anchor evidence→items/slots→validate→review→complete→reopen. Non-goals held: no PM-006..010 methods yet, no DataLab/shell/persona/collaboration, no schema, no network/LLM. Regression: full local suite 2794 total / 2792 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the TBJ-FINAL baseline 4d26d31 to the PM-005 implementation SHA 799b971 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim; the floor is corrected to the hosted totalTestCount if the metric differs). Continuous directive: PM-005 → PM-006 (Hypothesis Matrix + Evidence Collection Plan) → PM-007..010 → Workbench → shell → personas → release. PRIOR: 2762 = 2698 (MMI-FINAL hosted actual) + 64 (TBJ-FINAL Time-Bounded Job core — Stage A of the product-closure directive; 64 tests authored, none parameterized: JobObjectiveMigrationTests 21 + JobRepositoryTests 14 + JobPlanningServiceTests 15 + JobPlanningWorkflowLinkTests 7 + TBJBoundaryTests 7). SCHEMA v90 → v91 (ONE bump): three NEW tables — job_objectives + job_plan_references + job_events. A Job is a DURABLE PLANNING ENVELOPE over the existing task / deadline / workflow authorities, NOT a second task or deadline system: every plan reference points at a ProfessionalTask / WorkflowRun / step / requirement / evidence requirement / expected artifact that ALREADY exists in the canonical engine, and progress / priority / the time-bounded outcome are DERIVED at plan time from those live authorities — never stored here as a competing truth, never as a fabricated completion percentage. Time-budget integrity: the only deadline a job may bind as its budget basis is a row in `deadlines` — the CONFIRMED table (candidates live in the separate `deadline_candidates` table) — resolved at write time by JobRepository, so a DeadlineCandidate can NEVER become a job's authoritative budget; the planning service surfaces an unconfirmed candidate as \"possible deadline — needs confirmation\", never a countdown. Deadline / workflow pointers are SOFT references (validated by the repository, the method_runs idiom), so no ON DELETE action can null a budget column out from under the basis-integrity CHECK; ownership edges (workspace, owning job) are hard FK + CASCADE. CHECKs: nonblank title/actor/reference_id, budget-basis exclusivity (exactly the column matching the basis is populated; explicitDuration>0), closed lifecycle vocabulary {active,closed,abandoned} DISTINCT from ProfessionalTaskStatus/WorkflowRunStatus, active⇔no closed_at, revision/sequence>=1, closed reference_kind + role + MAD-flag vocabularies; COALESCE-unique plan-reference dedup; per-job unique event sequence. Self-heal sentinel gains the v91 markers; legacy pre-v91 data preserved with no fabricated jobs; migration-matrix group floor 242 → 263 (JobObjectiveMigrationTests, 21). DOMAIN (Kalsmritikosh/Jobs/): TimeBudget (+ JobBudgetBasis explicitDuration/confirmedDeadline/workflowConstraint/none, confirmed-only, well-formedness), JobObjective / JobPlanReference (+ JobPlanReferenceKind ×6, role ×3) / JobExecutionPlan / MinimumAcceptableDeliverable / JobEvent / JobRecord, and the DERIVED planning value types (TimeBudgetStatus, JobReferenceState, JobPriorityBand [hardBlocker>confirmedDeadlineCritical>requiredDeliverableDependency>decisiveEvidence>requiredReview>optionalEnrichment], JobProgressSnapshot [honest counts, NO percentage], MinimumDeliverableStatus, TimeBoundedOutcome [marksJobComplete always false]). REPOSITORY (JobRepository actor): atomic per-mutation SAVEPOINT + optimistic revision CAS, reference existence + same-workspace validation (task/run/step resolved to rows; requirement/artifact validate their context run), append-only events, human-recorded close/abandon/reopen (never inferred from underlying task/workflow completion), durable fetch() reconstructing the exact plan + history for resume after relaunch. SERVICE (JobPlanningService): derives (never persists) the honest outcome from the LIVE ProfessionalTask / Deadline / WorkflowRun authorities — confirmed-only countdown, candidate→advisory, expired→overdue, explicit-budget remaining from job start, dependency-aware blocking (an incomplete blocking predecessor makes the dependent blocked), deterministic priority bands (a fixed function of state + role + confirmed-deadline risk, NEVER of evidence confidence), minimum-deliverable met/notMet/undefined, and a partial outcome (completedNow / remainingWork / missingEvidence / blockedActions / recommendedNextAction) that NEVER marks the job complete. Wired into AppState (jobs: JobRepository?), live from boot over the shared ledger; the persona-shell \"My Work\" surface consumes it in a later stage (Stage D). Non-goals held: NO concrete professional methods, NO DataLab, NO persona shell/UI, NO second task/deadline/workflow/readiness/SensitiveScope authority, NO network/LLM, NO fake epistemic percentage. Regression: full local suite 2780 total / 2778 passed / 2 skipped / 0 failed; the prior MMI boundary guard relaxed to >= 90 for the v91 bump (the recurring prior-unit-pin lesson); MigrationMatrixTests pins latest v91. Two-run acceptance protocol: baselineCommit advances from the MMI-FINAL baseline 83747b0 to the TBJ-FINAL implementation SHA 4d26d31 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim; the floor is set from the local totalTestCount and corrected to the hosted totalTestCount if the metric differs, per the MMI-FINAL lesson). Owner-locked sequence: TBJ (this) → Concrete Professional Methods, do NOT combine. PRIOR: 2698 = 2628 (AEE-FINAL hosted actual) + 70 (MMI-FINAL hosted totalTestCount delta; 71 test functions authored — the metric counts +70) (MMI-FINAL Multimodal Meaning & Identity: 71 tests authored — TypedFieldMigrationTests 15 + TypedFieldExtractorTests 13 + TypedFieldRepositoryTests 9 + MMIIngestionIntegrationTests 5 + IdentityFieldResolverTests 12 + MMIIdentityFastPathTests 7 + MMIBoundaryTests 10). SCHEMA v89 → v90 (ONE bump): NEW table typed_fields — a deterministic identity/document field (personName/documentNumber/issueDate/email/amount/…) pinned to the EXACT evidence_block_id + source_version_id + locator it came from; FK cascade from BOTH source_versions and evidence_blocks; CHECKs on confidence[0..1], ocr_confidence null-or-[0..1], nonblank field_type + raw_value. The FIRST accepted producer for the USF-004 typedFields content surface AND the typedFieldExtraction readiness dimension (both previously reserved). A typed field is NOT a Claim — no claim promotion. NO SECOND parser/EvidenceBlock/readiness/SensitiveScope authority: all multimodal output still enters through the ONE UniversalParserRegistry → EvidenceBlocks → SourceLocator → readiness. Deterministic TypedFieldExtractor (labeled + pattern rules over the ALREADY-ACCEPTED EvidenceBlocks; NO model call, NO network, NO guessing; confidence scaled by the block's extraction method native>OCR/vision; boilerplate skipped; low-confidence header-name heuristic never asserts). TypedFieldRepository (atomic per-(version,producer,producerVersion) replace; exact-version reads; conflicts surfaced both-preserved-never-resolved). Wired into IngestCoordinator (runs over the persisted blocks after a structural parse; advances typedFieldExtraction readiness with an evidence-block basis when fields exist; best-effort; a doc with no identity fields extracts nothing and does not falsely satisfy readiness) + ContentSurfaceProjector (real typedFields surface receipt). IdentityFieldResolver (maps an identity/document question to its field type; one dominant value → deterministic 0-LLM answer; several distinct → candidates, never a guess; below confidence floor → not found). MasterBrain.identityFieldFastPath (before tableFastPath; reads located values, gates each by its owning KnowledgeObject via the EXISTING SensitiveScope authority [effectiveLabel + access.scope.permits — no second sensitive-data state], returns a deterministic cited VerifiedAnswer or candidates, else nil). Media stays honest — no network/transcription shortcut added (media remains deferred/PreservedOnly per USF; no URLSession/SFSpeech/WhisperKit in MMI). Non-goals held: no TBJ job planning, no concrete professional methods, no DataLab, no second authority of any kind. Legacy pre-v90 data preserved (no fabricated fields); self-heal sentinel gains the v90 marker; migration-matrix group floor 228 → 242 (TypedFieldMigrationTests, 15). Regression: full local suite 2716 total / 2714 passed / 2 skipped / 0 failed (2 already-merged AEE guards relaxed to >= for the v90 bump — the recurring prior-unit-pin lesson). Two-run acceptance protocol: baselineCommit advances from the AEE-FINAL baseline 9d98f28 to the MMI-FINAL implementation SHA 83747b0 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). Owner-locked sequence: MMI (this) → TBJ → Concrete Methods, do NOT combine. PRIOR: 2628 = 2544 (AEE-M1 hosted actual) + 84 (AEE-FINAL = AEE-M2 Progressive Answer Contract + Explicit Correction / Revision Ledger + Durable Final-Answer Audit + AEE closure/evaluation + minimal answer-history UI + legacy answer-state path removal; the +84 mandated floor; 85 tests authored: ProgressiveAnswerMigrationTests 15 + ProgressiveAnswerStateMachineTests 14 + AnswerRevisionLedgerTests 15 + ExplicitCorrectionTests 10 + AEEM2MasterBrainIntegrationTests 9 + AEEM2BoundaryTests 10 + AEEFinalEvaluationTests 12). SCHEMA v88 → v89 (ONE bump): EXTENDS the existing answer-ledger authority (answers/answer_claims/claim_evidence, v28) — NO second answer store — with two NEW tables: answer_revisions [immutable per-answer revision chain; UNIQUE(answer_id,revision_number); SHA-256-shaped content_hash CHECK matching ProgressiveAnswerContentHasher; a correction needs a prior revision + nonblank reason AND, via the (correction_of_revision_id,answer_id)→answer_revisions(id,answer_id) composite FK backed by UNIQUE(id,answer_id), can only reference a revision of the SAME answer; never updated/deleted] + answer_revision_events [append-only lifecycle ledger, UNIQUE(answer_id,sequence), closed 7-state vocabulary immediateFinding/groundedWorkingResult/analysisProgress/reviewReady/verifiedFinal/corrected/incomplete, content-bearing-requires-revision CHECK (analysisProgress + incomplete may be revision-less)]; plus nullable answer_claims.revision_id + nullable answers compat cols (request_id/mission_lane/mission_objective/mission_deliverable/is_terminal/updated_at). Legacy pre-v89 answers preserved verbatim with NO fabricated revision history (no backfill); self-heal sentinel gains v89 markers; migration-matrix group floor 214 → 228 (ProgressiveAnswerMigrationTests, 15). DOMAIN (Brain/AEE/): ProgressiveAnswerState (7 closed, answer LIFECYCLE kept SEPARATE from the corpus-support AnswerState — the two are never merged), CorrectionReasonKind (8 closed; .other requires detail; .userCorrection stays user-attributed), ProgressiveAnswerContentHasher (deterministic SHA-256 over normalized text + sorted citation identities + AnswerState — reflow-stable, a real change → new hash), ProgressiveAnswerStateMachine (legal order + terminality). REPOSITORY (AnswerLedgerRepository+Revisions): beginAnswer/appendFinding/appendWorkingResult/appendProgress/recordCorrection/markReviewReady/lockVerifiedFinal/markIncomplete — each transition in ONE savepoint (revision+claims+evidence+event+projection); every new claim pinned to its exact revision; deterministic replay answer→revisions→claims→evidence→EvidenceBlocks→SourceVersions (no LLM); recoverInterruptedAnswers marks non-terminal v89 answers incomplete(interrupted) preserving the last durable revision (legacy answers untouched, wired at AppState boot). MASTERBRAIN: AnswerUpdate gains the 7 lifecycle states (5 legacy cases retained for compile compat only — production emits none, proven by AEEM2BoundaryTests); answerStream emits analysisProgress for cached memory + reconstruction chapters and finalises via finalizeProgressiveAnswer, which commits working-result→review-ready→verifiedFinal in the ledger BEFORE emitting verifiedFinal (a persistence failure or refused/uncited answer emits incomplete, never a final); the legacy fire-and-forget persistAnswerLedger is removed; the answer() wrapper collects verifiedFinal/incomplete. Minimal AnswersView revision-history UI (lifecycle badge + per-revision correction reason; legacy answers show nothing). Non-goals held: no MMI/TBJ/DataLab, no concrete professional method, no second AnswerState/answer authority, no shell redesign. Regression: full local suite 2646 total / 2644 passed / 2 skipped / 0 failed (2 already-merged AEE-M1 guards updated for v89 + the M2 lifecycle). Two-run acceptance protocol: baselineCommit advances from the AEE-M1 baseline 97d4d12 to the AEE-FINAL implementation SHA 9d98f28 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2544 = 2492 (USF-FINAL hosted actual) + 52 (AEE-M1 Query Mission + Adaptive Evidence Lanes — the mandated floor removes the accumulated slack by anchoring on the last hosted ACTUAL 2492 rather than the 2443 floor; 52 tests authored: QueryMissionCompilerTests 12 + MissionLanePolicyTests 10 + MissionEvidenceObligationTests 12 + AEEMasterBrainIntegrationTests 10 + AEEM1BoundaryTests 8). NO SCHEMA CHANGE (stays v88; migration-matrix group floor UNCHANGED at 214) — AEE-M1 is orchestration/domain code only. New subsystem Kalsmritikosh/Brain/AEE/: ONE QueryMission per request DERIVED (no re-analysis, no model call) from the existing authorities (UserIntent, QueryCategory, LLMQueryClass, QueryPlan) via QueryMissionCompiler — objective (8 closed) / deliverable (9 closed) / evidenceRisk / effortClass / primaryLane (AEELane ×5: deterministic/focused/analytical/reconstruction/professionalWorkflow) / MissionEvidenceObligations / bounded budget (allowedLLMCalls = lane ceiling ∩ LLMQueryClass.callLimit, focused ≤1, deterministic 0 — never larger than the existing hard ceiling). MissionLanePolicy locked precedence workflow > reconstruction > analytical > deterministic-handler > focused (text alone never reaches professionalWorkflow). MissionEvidenceObligations + MissionReadinessFloor {searchReady|evidenceReady} reference the USF completion truth (NOT a second readiness system). MissionEvidenceAssessor wraps the existing EvidenceSufficiency + USF completion states + corroboration into satisfied/partial/blocked/unsupported — a permanently-blocked decisive source on a high-risk lane BLOCKS (never answered around); duplicates never corroborate; contradictions are reported not resolved. AdaptiveEvidencePlanner + AEEEvidenceUpgrading bridge (IngestCoordinatorEvidenceUpgradeBridge over the USF exact-version upgrade queue) raise ONLY the decisive retrieved source versions to the mission floor (never the whole archive; best-effort + exact-byte protected). MasterBrain.computeVerified now compiles intent/category/plan/mission ONCE (category no longer recomputed for the trace; corrective retrieval uses the request's REAL plan, was hardcoded .fact/.ordinary), runs the adaptive evidence lane between first retrieval and the single corrective pass when a bridge is wired (no-op when nil → every existing path byte-for-byte unchanged), and stamps mission telemetry onto ReasoningTrace (optional backward-compatible fields). AppState attaches the bridge post-boot. Non-goals held: no AEE-M2 progressive-answer states, no MMI/TBJ/DataLab, no concrete professional method, no second readiness/completion/query-plan/trace/sufficiency authority, no persistence, no schema, no network. Regression: existing query consumers (CorrectiveRetrieval[Planner], IntentClassification, QueryPlanCompiler, QueryCategoryClassifier, EvidenceSufficiency, EvidencePrompt, EvidenceDecisionWiring, LLMBudget, DeterministicReconstruction) all green; full local suite 2562 total / 2560 passed / 2 skipped / 0 failed. Two-run acceptance protocol: baselineCommit advances from the USF-FINAL baseline 037ee13 to the AEE-M1 implementation SHA 97d4d12 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2443 = 2367 (USF-M2) + 76 (USF-FINAL — Universal Source Foundation Closure = USF-008 Canonical Ingestion Completion Contract + USF-009 Progressive Ingestion / On-Demand Upgrade + USF-010 Integrity / Recovery / Reprocessing, delivered on ONE branch, ONE schema bump, TWO hosted CI runs; the +76 mandated floor; 86 tests authored: ProgressiveUpgradeMigrationTests 13 + CanonicalIngestionCompletionTests 13 + SourceUpgradeJobRepositoryTests 14 + SourceUpgradePlannerTests 10 + SourceVersionByteResolverTests 6 + ProgressiveIngestIntegrationTests 10 + SourceReprocessingTests 8 + USFFINALBoundaryTests 12). SCHEMA v87 → v88 (ALL USF-008/009/010 persistence in the one v88 migration): enrichment_jobs REBUILT into an exact-SourceVersion background-work queue [scope_kind legacySubject|sourceVersion; a sourceVersion job carries a non-null source_version_id pinned by FK to source_versions; closed state pending/running/done/failed/blocked/cancelled/superseded; running-requires-lease CHECK; priority + not_before + lease_token/lease_expires_at columns; producer_id/producer_version; partial UNIQUE idx_enrichment_jobs_active_sv gives ONE active job per (source_version_id,kind,producer_id,producer_version) WHERE scope='sourceVersion', and the legacy (subject_id,kind) UNIQUE is preserved for legacySubject rows] + append-only enrichment_job_events [closed action vocabulary, UNIQUE(job_id,sequence), CASCADE on job delete]; legacy rows preserved verbatim as legacySubject with a NULL source_version_id (never guessed); the self-heal sentinel gains v88 column/table markers; a job describes WORK, never readiness. USF-008 (Ingestion/Completion/): IngestionCompletionSnapshot + pure IngestionCompletionEvaluator + IngestionCompletionService reconstruct one canonical completion per EXACT SourceVersion DERIVED ONLY from the durable readiness snapshot (SourceReadinessSnapshot/SourceCompletionState reused verbatim — no second completion vocabulary, no percentage) overlaid with the container-inspection status and the pending/running/failed upgrade kinds; ingest-attempt status can never override it. USF-009 (Ingestion/Upgrade/): SourceProcessingIntent {initialFast|fullAvailable|evidenceUpgrade|analyticalUpgrade} makes the *app* faster — the initial pass takes custody + searchable text + chunks + FTS + readiness and RETURNS searchable (never blocking on evidence/analytical), while an on-demand SourceUpgradeCoordinator plans the MINIMAL missing kinds (SourceUpgradePlanner reuses already-satisfied work — an already-evidence-ready goal schedules nothing), leases them on the exact SourceVersion (SourceUpgradeJobRepository claim/succeed/fail-bounded-retry/failTerminal/block/cancel/supersede/recoverExpiredLeases, priority-ordered), reopens the EXACT bytes (SourceVersionByteResolver — managed reopens the hash-verified vault blob; referenced re-hashes the current file and refuses a changed source with sourceBytesChanged so a mutated file can never mutate the old version), parses via the ONE UniversalParserExecutor at intent .evidenceStructure, and marks a job done ONLY when its readiness postcondition holds (else failTerminal — never silently done); background drains yield to interactive queries via QueryPriorityGate. USF-010 (Ingestion/Upgrade/SourceReprocessingCoordinator): staleness is detected per exact SourceVersion by comparing a parser-dependent dimension's producer_version to the current parser version; reprocess() re-verifies the exact bytes first (throws for changed/missing bytes), then invalidates ONLY the stale parser dimensions (structural/metadata/OCR) via the ready→running invalidate transition and re-stamps them to the prior state with the new producer version — preserving custody (the source_versions row is untouched), search readiness (loader-produced dimensions untouched) and unrelated accepted work; idempotent (a fresh or converged source is .upToDate). Production ingest now records ingest_file_attempts status `.passCompleted` (operational — the pass ran) rather than the legacy `.queryable`; completion still reflects readiness, not the attempt. IngestCoordinator makes ONE plugin decision, splits the readiness advance (search dims default producer; structural/metadata/ocr stamped producerID 'usf-m3.structural' + the plugin version), attaches the completion snapshot, schedules only the missing search/evidence background work, and exposes ensureUpgrade/drainUpgrades/completion/reprocess; AppState constructs SourceUpgradeJobRepository, configures the upgrades, and recovers leases stranded by a prior crash/quit at boot. Non-goals held: NO second source/version/readiness/completion authority, NO Claim promotion, NO AEE/MMI/TBJ/concrete-method work, NO OCR/transcription engine activation, NO network/LLM, NO UI redesign. Migration-matrix group floor rose 201 → 214 (ProgressiveUpgradeMigrationTests, 13). Two-run acceptance protocol: baselineCommit advances from the USF-M2 baseline 4b28324 to the USF-FINAL implementation SHA 037ee13 (which corrects the v88 running-requires-lease CHECK to sourceVersion scope only, preserving the legacy enrichment-job claim contract — the fix for the first PR-head run's single regression) in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2367 = 2311 (USF-M1) + 56 (USF-M2 Safe Container Expansion + Case Coverage Manifest — the +56 mandated floor; 93 tests authored: ContainerManifestMigrationTests 14 + CompoundContainerDetectionTests 11 + ContainerSafetyTests 21 + ContainerInspectionRepositoryTests 8 + StableMemberIntakeTests 4 + ContainerIngestIntegrationTests 15 + CaseCoverageManifestTests 10 + USFM2BoundaryTests 10). USF-M2 combines USF-006 (safe container handling) + USF-007 (case coverage manifest). SCHEMA v87 (two PROCESSING-PROJECTION tables, NOT source/evidence/readiness authorities): container_manifests [one current inspection manifest per exact container SourceVersion; closed status complete/partial/blocked/unsupported/failed; count-consistency CHECK admitted+blocked+unsupported+failed <= regular_file_entries] + container_members [every member disposition incl. never-admitted; closed disposition vocabulary blockedUnsafePath/blockedDepth/blockedEntryLimit/blockedSizeLimit/blockedCompressionRatio/blockedRootBudget/blockedCycle/encrypted/unsupported/failedExtraction/directory/admitted; admitted requires child+hash pinned to source_versions(id,content_hash); non-admitted forbids a fabricated child; UNIQUE(parent,ordinal), duplicate paths allowed]; self-heal sentinel gains the v87 markers; migration-matrix group floor 187 -> 201 (ContainerManifestMigrationTests, 14). UPSTREAM FIX (no USF-M1 reopen): compound-container type detection — DOCX/XLSX/PPTX/ODT/ODS/EPUB are ZIPs, so the ONE intake detector now disambiguates the logical subtype via declared extension (basis stays magicBytes) instead of mis-detecting a real DOCX as .zip; added RAR4/RAR5/7z signatures. New subsystems Ingestion/Container/ + Ingestion/Coverage/. Container handling: ONE immutable ContainerSafetyPolicy (per-container entry/byte/member caps + root-recursion ceilings: depth 8, root member/byte budgets, nested-container count, compression ratio) recorded via policy_version; ZIPContainerExtractor STREAMS each member to a temp file with a running per-member byte cap (a 1 GiB member is never held whole; STORED windowed, DEFLATE chunk-inflated) + correct path-traversal handling (reject only real escapes; a filename containing '..' is safe); ZIPContainerInspector enumerates + classifies every member into exactly one disposition (none silently dropped); ContainerProcessingCoordinator threads a SHARED root budget (not reset per nested container) + ancestor-hash cycle detection + depth ceiling, hands each admitted member to the FULL pipeline via IngestCoordinator (managed custody + a stable kalsmritikosh-container:// origin so a temp extraction path never becomes durable identity; universal parser, never a bypass), records the manifest + every disposition, and recurses into nested ZIP children within the shared limits; RAR/7z stay honest-unsupported (custody kept, contents not enumerated, unknown != empty; no shell/Homebrew/bundled decoder). ContainerInspectionRepository is the SOLE v87 writer (one savepoint: verify container SourceVersion + compatibility, CAS revision, atomic manifest+member replace, derived counts, admitted-child invariants). Case Coverage (USF-007): CaseCoverageManifest is a DETERMINISTIC SNAPSHOT reconstructed from existing authorities (workspace_sources / files+alias_of / source_versions / source_version_relations / source_readiness_* / container_manifests+members) — no second membership or readiness system, no writes, no evidence copies, no overall percentage; the coverage closure covers archiveMember/attachment/message/embedded descendants automatically (derivedConversion excluded), a source reached by several paths is counted once with its occurrence paths preserved, unadmitted/not-inspected members stay visible, readiness reuses SourceReadinessSnapshot/SourceCompletionState verbatim, and two identical DB states yield an identical manifest apart from generatedAt. Non-goals held: no USF-008+/USF-010, no nested RAR/7z decoders, no network/LLM, no Claim/method mutation, no UI redesign. Two-run acceptance protocol: baselineCommit advances from the USF-M1 baseline 7077ea1 to the USF-M2 implementation SHA 4b28324 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2311 = 2263 (USF-002.1) + 48 (USF-M1 Universal Parser Platform — the +48 mandated floor; 50 tests authored: UniversalParserRegistryTests +1 -> 14 [manifest-from-registry] + UniversalContentSurfaceTests 13 + UniversalParserIntegrationTests 14 + USFM1ParserPlatformBoundaryTests 9). USF-M1 combines USF-003 + USF-004 + USF-005 and introduces NO schema change (stays v86; migration-matrix group floor UNCHANGED at 187). It unifies the two previously independent routing authorities — StructuralParserRegistry (SourceType->StructuralParser) and LoaderRegistry (SourceType->Ingestor + TextLoader fallback) — into ONE immutable UniversalParserRegistry where exactly one UniversalParserPlugin owns each SourceType (duplicate id / duplicate type-ownership / blank identity / loader+structural type mismatch all FAIL at construction; never last-wins). Plugins consume the USF-001.2 immutable processing snapshot (never the mutable original); the UniversalParserExecutor fail-closes each result against the request identity (logicalSourceID / sourceVersionID / sourceType / contentHash) AND independently against the ParsedDocument's own identity (the exact-byte parser-hash == version-hash gate now lives here as contentHashMismatch; a structure-less plugin that smuggles a document throws unexpectedStructuralOutput). USF-004 content surfaces (8 closed kinds: text/metadata/structure/tables/images/attachments/transcript/typedFields; coverage complete/partial/notApplicable ONLY — NOT a second readiness system; typedFields stays notApplicable until an accepted producer) project over the EXISTING EvidenceBlock model via ContentSurfaceProjector. ExistingParserPluginAdapter wraps each existing Ingestor (+ optional StructuralParser) WITHOUT rewriting algorithms, reads the snapshot ONCE, runs the loader ONCE via ingestMany, and runs the structural parser over the same bytes. Media (audio/video) stays PreservedOnlyPlugin(deferred) — no AudioLoader/VideoLoader/SpeechTranscriber activated; archives declare container mode; .unknown routes to an explicit system.generic-text-fallback (honest — no fabricated text/blocks for binary). IngestCoordinator makes ONE SourceType->plugin decision and the loader executes ONCE (via the adapter's ingestMany, never both ingest + ingestMany); it no longer consults LoaderRegistry / StructuralParserRegistry (proven by USFM1ParserPlatformBoundaryTests, which allow documentation mentions but forbid CALLS). Durable readiness STILL derives only from committed proof (StructuralPersistenceReceipt + ftsCoverage); no plugin declares isEvidenceReady/isSearchReady/isAnalyticallyReady. ParserCapabilityManifest.generate(registry: UniversalParserRegistry) derives the coverage matrix from the ONE registry. Non-goals held: no v87, no USF-006 (nested archive recursion / RAR / 7z), no OCR/transcription engines, no typed-field producer, no Claim promotion, no network/LLM, no UI redesign. Two-run acceptance protocol: baselineCommit advances from the USF-002.1 baseline 49fa33c to the USF-M1 implementation SHA 7077ea1 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2263 = 2231 (USF-002) + 32 (USF-002.1 Durable Readiness Proof and Exact-Version Coverage: ExactVersionChunkMigrationTests 9 + DurableReadinessProofTests 15 + DurableReadinessIngestTests 8). USF-002.1 adds schema v86 (chunks.source_version_id — a retrieval projection field, soft column + index, with a PROVABLE evidence-block backfill; legacy unprovable chunks stay NULL), a committed StructuralPersistenceReceipt returned by EvidenceStore.persist (readiness advances structure/metadata/OCR ONLY from it — a persistence failure throws → structure FAILED, never ready on in-memory blocks), structural readiness gated on extractionStatus==complete AND every substantive block located (a partial parse or a missing locator stays partial), exact per-version FTS coverage (SourceReadinessRepository.ftsCoverage reconstructed from persisted chunks — a parent's indexing readiness never counts a child attachment's chunks), and repository hardening (positive structural/index readiness requires a durable basis; indexing coverage units must equal the DB-derived coverage; soft ftsIndex/vectorIndex/custody bases must name THIS exact version; empty plans rejected; invalidation requires a producer-version or basis change, not just a reason). ParseCoverageReport.from(_ doc:) is now INTERNAL (non-authoritative); the durable path is SourceReadinessRepository → SourceReadinessSnapshot → ParseCoverageReport/SourceReadinessSummary. Migration-matrix group floor rose 178→187 (ExactVersionChunkMigrationTests, 9). Two-run acceptance protocol: baselineCommit advances from the USF-002 baseline 0ac08d7 to the USF-002.1 implementation SHA 49fa33c in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (no green claim here; the PR-head run id + counts are recorded in the PR after the hosted run). PRIOR: 2231 = 2159 (USF-001.2) + 72 (USF-002 Independent Source Readiness Dimensions: SourceReadinessMigrationTests 17 + SourceReadinessEvaluatorTests 16 + SourceReadinessRepositoryTests 18 + SourceReadinessIngestIntegrationTests 12 + USF002ReadinessBoundaryTests 9). USF-002 adds schema v85 (source_readiness_aggregates + source_readiness_dimensions [ten independent closed dimensions per EXACT source version: preservation/metadata/text/structural/ocr/transcription/indexing/basicQA/typedFields/analytical] + append-only source_readiness_events; conservative v84→v85 backfill from canonical evidence; self-heal column probe), the deterministic SourceReadinessEvaluator (search ≠ evidence ≠ analytical; completion state DERIVED, never stored/caller-set; no overall percentage), the atomic SourceReadinessRepository (one savepoint: existence + optimistic CAS + validation + dimension writes + one aggregate revision bump + contiguous events + snapshot rebuild; closed transition rules; invalidation), intake bootstrapping the ten dimensions in the SAME transaction that creates a new source version (unchanged/moved/aliased reuse the canonical readiness — no second aggregate), pipeline advancement of text/indexing/structure from durable representations, and SourceReadinessSummary/ParseCoverageReport refactored to consume the durable authority (live IngestProgress stays separate). No second source/version authority; readiness never confirms a Claim. Migration-matrix group floor rose 161→178 (SourceReadinessMigrationTests, 17). Non-goals (deferred): parser-registry work (USF-003+), OCR/transcription engines, typed-field extraction algorithms, reprocessing UX (USF-010), major Sources UI redesign, network/LLM, Claim promotion. Two-run acceptance protocol: baselineCommit advances from the USF-001.2 baseline 624e21b to the USF-002 implementation SHA 0ac08d7 in this stacked evidence-manifest commit; acceptance is CONDITIONAL on all six PR checks passing (the PR-head run id + exact counts are recorded in the PR after the hosted run — this commit makes no green claim). PRIOR: 2159 = 2135 (USF-001.1) + 24 (USF-001.2 Exact-Byte Binding and Alias Transition Closure: ExactByteBindingMigrationTests 9 + ExactByteBindingIntakeTests 8 + AliasTransitionClosureTests 7). USF-001.2 adds schema v84 (source_versions rebuilt so a non-legacy custody hash must be HEXADECIMAL — `content_hash NOT GLOB '*[^0-9a-f]*'` closes the v83 gap that accepted any 64-char lowercase string; the migration demotes existing non-hex custody rows to legacyImported; self-heal gains the v84 GLOB probe), an immutable per-intake processing snapshot (SourceByteCapture.captureToSnapshot streams the exact bytes into a temp snapshot in the SAME verified pass as the hash; the loader and structural parser consume ONLY the snapshot; the parser's own hash therefore equals the source version's hash — the USF-001.1 foreign-hash substitution is REMOVED and a mismatch writes no structural artifacts; the coordinator removes the snapshot after processing and rebinds KO identity to the original url), alias transition closure (an alias URL whose bytes change is either re-pointed at another canonical reusing its version, or promoted in place to its own canonical source — never a second file row for the URL), and managed known-hash vault reuse (a managed version whose hash is already vaulted reuses that verified address; managedCopyStored is derived from an actual present address, never from the absence of a copy failure). Migration-matrix group floor rose 152→161 (ExactByteBindingMigrationTests, 9). baselineCommit = 624e21b (USF-001.2 hosted-green implementation SHA, CI run 30678478955, all 6 jobs green: build-and-test 2159 executed / 2157 passed / 2 skipped / 0 failed (floor 2159); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the USF-001.1 baseline 181bf0f to 624e21b. PRIOR: 2135 = 2103 (USF-001) + 32 (USF-001.1 Single-Path Intake and Custody Integrity: SinglePathIntakeIntegrityMigrationTests 11 + USF001SinglePathIntegrityTests 16 + 5 new IngestCoordinatorSafeIntakeTests terminal-attempt tests). USF-001.1 adds schema v83 (source_versions non-legacy SHA-256 + same-logical supersedes composite FK + UNIQUE(id,logical_source_id)/(id,content_hash); source_intake_receipts composite FKs pinning the receipt to the version's logical source AND hash; ingest_file_attempts both-or-neither + composite FK), makes the intake coordinator MANDATORY (legacy ingest branch removed — intake is the sole pre-parser identity authority), makes EvidenceStore ATTACH-ONLY in one savepoint (fail-closed on version/logical/hash/document-id mismatch; never creates or retires a version), single-pass verified vault storage (bytes hashed while copied → address always matches content), ONE authoritative type detector (path pattern → magic → declared extension → unknown; IngestCoordinator uses handle.detectedType), fully atomic in-savepoint identity resolution + existing-alias re-intake, and ONE version-linked terminal attempt per ingest. Migration-matrix group floor rose 141→152 (SinglePathIntakeIntegrityMigrationTests, 11). baselineCommit = 181bf0f (USF-001.1 hosted-green implementation SHA, CI run 30644290285, all 6 jobs green: build-and-test 2135 executed / 2133 passed / 2 skipped / 0 failed (floor 2135); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the USF-001 baseline a176362 to 181bf0f. PRIOR: 2103 = 2047 (PM-004.1) + 56 (USF-001 Universal Safe Intake: UniversalSourceIntakeMigrationTests 14 + CanonicalSourceIntakeRepositoryTests 20 + IngestCoordinatorSafeIntakeTests 13 + USF001UniversalSafeIntakeBoundaryTests 9). USF-001 adds schema v82 (source_versions rebuilt with intake custody metadata — filename/detected type/detection_basis/size/custody_mode/preservation_status/vault_address/intake_recorded_at, NOT-NULL with DEFAULTs so legacy short-form inserts still round-trip; content_hash left unconstrained as pre-v82; one-current partial UNIQUE index; source_intake_receipts + source_version_relations + version-linked ingest_file_attempts), the intake subsystem (streaming SHA-256 capture, CanonicalSourceIntakeRepository atomic identity transaction, UniversalSourceIntakeCoordinator, EvidenceVault.storeStreaming), and the IngestCoordinator pre-parser rewire (custody before loader; unchanged/moved/aliased skip parsing; loader/parser failure keeps custody; EvidenceStore attaches the parsed document to the intake-created version). No second source/version authority; source_versions remains the one version authority. Migration-matrix group floor rose 127→141 (UniversalSourceIntakeMigrationTests, 14). baselineCommit = a176362 (USF-001 hosted-green implementation SHA, CI run 30637463242, all 6 jobs green: build-and-test 2103 executed / 2101 passed / 2 skipped / 0 failed (floor 2103); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the PM-004.1 baseline 27c7ced to a176362. PRIOR: 2047 = 2030 (PM-004) + 17 (PM-004.1 Lifecycle Ledger and Migration Hardening: MethodLifecycleLedgerHardeningMigrationTests 9 + MethodLifecycleEpochEnforcementTests 8). PM-004.1 adds schema v81 (rebuilds method_runs for the REVERSE supersession CHECK, method_reviews for non-blank review_key + non-negative reviewed_content_revision + not-both-node-and-finding CHECKs, method_validation_results for non-blank validation_batch_id + non-negative evaluated_content_revision CHECKs — NO new column), makes the migration pass atomic (a single OUTER savepoint wraps every pending migration AND the final PRAGMA foreign_key_check, so a post-pass FK violation rolls the whole pass + user_version back), and makes applyLifecyclePlan self-enforce gate epochs (a review must evaluate the effective content revision — current, or current+1 on reopen; a validation batch must be one non-legacy batch, every row owned by the run and evaluating the current revision). The self-heal sentinel gains v81 CHECK-text probes (v81 adds no column, so the column probe cannot distinguish v80). Migration-matrix group floor rose 118→127 (MethodLifecycleLedgerHardeningMigrationTests, 9). baselineCommit = 27c7ced (PM-004.1 hosted-green implementation SHA, CI run 30628380062, all 6 jobs green: build-and-test 2047 executed / 2045 passed / 2 skipped / 0 failed (floor 2047); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the PM-004 baseline 369a4b3 to 27c7ced. PRIOR: 2030 = 1950 (PM-003) + 80 (PM-004 Generic Method Lifecycle, Validation, Review and Durable Reopen: MethodLifecycleStateMachineTests 15 + ProfessionalMethodValidationTests 12 + MethodReviewGateTests 12 + ProfessionalMethodLifecycleEngineTests 19 + PM004MethodLifecycleBoundaryTests 9 + MethodLifecycleMigrationTests 13). Prior-unit PM-001/002/003 tests the v80 bump touched were updated in place (no count change). PM-004 adds schema v80 (method_runs rebuilt for content_revision + 'paused'; method_evidence_links.input_role; method_reviews.review_key + reviewed_content_revision; method_validation_results.validation_batch_id + evaluated_content_revision; new method_run_events durable audit table) + the closed lifecycle state machine, deterministic validator runtime, definition-keyed human-only review gates, completion gates, human-authorized durable reopen (a distinct contentRevision epoch invalidates prior validation/review), and terminal immutability. Content mutations are gated to draft/active and bump content_revision via the same optimistic-revision CAS. The migration disables foreign-key enforcement for the whole pass (SQLite-recommended for a parent-table rebuild) + runs PRAGMA foreign_key_check. No concrete method (Five Whys/Fishbone/CAPA/…), UI, LLM, network or Claim promotion. Migration-matrix group floor rose 105→118 (MethodLifecycleMigrationTests, 13). baselineCommit = 369a4b3 (PM-004 hosted-green implementation SHA, CI run 30624772397, all 6 jobs green: build-and-test 2030 executed / 2028 passed / 2 skipped / 0 failed (floor 2030); migration-matrix, sensitive-export, parser-fixtures, report-receipt-integrity, architecture-guards all green). This evidence commit advances baselineCommit from the PM-003 baseline 68bd22a to 369a4b3. PRIOR: 1950 = 1905 (PM-002) + 45 (PM-003 Professional-Method Registry + Stage-3 Adapter Bridge: ProfessionalMethodRegistryTests 16 + ProfessionalMethodWorkflowBridgeTests 10 + RegisteredMethodStepExecutorTests 10 + PM003MethodRegistryBridgeBoundaryTests 9). PM-003 adds NO schema change (stays v79) and NO lifecycle service (PM-004): one code-backed ProfessionalMethodRegistry (wraps the generic backbone; no definition table), reference-only bridge envelopes + ProfessionalMethodWorkflowBridge, and the v2 RegisteredMethodStepExecutor (same id, version 2, schema-2-bound; v1 + schema-1 untouched). Migration-matrix group floor stays 105 (PM-003 has no migration). PRIOR: 1905 = 1866 (PM-001) + 39 (PM-002 Method Persistence: MethodRunMigrationTests 15 + MethodRunRepositoryTests 15 + PM002MethodPersistenceBoundaryTests 9; the +4 over the first PM-002 pass are the owner-review corrections: evidence-link stable id, single-snapshot aggregate, mandatory non-run validation subject + its schema CHECK). PM-002 adds schema v79 (the 8-table MethodRun aggregate ledger) + the authoritative MethodRunRepository under Kalsmritikosh/Method/Persistence/. Stage-4 persistence only: no concrete method, lifecycle policy, Stage-3 bridge, UI, LLM, network or Claim promotion. The migration-matrix group floor also rose 90→105 (MethodRunMigrationTests, 15). Tests are cumulative. New tests raise it; the verifier fails when fewer execute or any test fails. Read from build/TestResults.xcresult — never from console text. baselineCommit = 68bd22a (PM-003 hosted-green implementation SHA, CI run 30608063827, all 6 jobs green: build-and-test 1950 executed / 1948 passed / 2 skipped / 0 failed; migration-matrix 105/14; sensitive-export 122/7; parser-fixtures 78/20; report-receipt-integrity 77/7; architecture-guards clean). PM-003 introduces NO concrete method and NO lifecycle service (PM-004). Prior PM-002 baseline 28b22dd (run 30605062552). Stage 3 (Persona Job Engine, PJE-001..012) COMPLETE — see STAGE3_ACCEPTANCE.md."
}
