{
  "group": "migration-matrix",
  "selectors": [
    "KalsmritikoshTests/MigrationMatrixTests",
    "KalsmritikoshTests/MigrationFaultInjectionTests",
    "KalsmritikoshTests/RealArchiveMigrationTests",
    "KalsmritikoshTests/EvidenceDimensionMigrationTests",
    "KalsmritikoshTests/ProfessionalIssueMigrationTests",
    "KalsmritikoshTests/ProfessionalTaskMigrationTests",
    "KalsmritikoshTests/SensitiveScopeMigrationTests",
    "KalsmritikoshTests/WorkProductRunMigrationTests",
    "KalsmritikoshTests/EmailParticipantMigrationTests",
    "KalsmritikoshTests/SourceReliabilityAssessmentMigrationTests",
    "KalsmritikoshTests/WorkflowRunMigrationTests",
    "KalsmritikoshTests/WorkflowProvenanceMigrationTests",
    "KalsmritikoshTests/WorkflowAutomationExecutionMigrationTests",
    "KalsmritikoshTests/MethodRunMigrationTests",
    "KalsmritikoshTests/MethodLifecycleMigrationTests",
    "KalsmritikoshTests/MethodLifecycleLedgerHardeningMigrationTests",
    "KalsmritikoshTests/UniversalSourceIntakeMigrationTests",
    "KalsmritikoshTests/SinglePathIntakeIntegrityMigrationTests",
    "KalsmritikoshTests/ExactByteBindingMigrationTests",
    "KalsmritikoshTests/SourceReadinessMigrationTests",
    "KalsmritikoshTests/ExactVersionChunkMigrationTests",
    "KalsmritikoshTests/ContainerManifestMigrationTests",
    "KalsmritikoshTests/ProgressiveUpgradeMigrationTests",
    "KalsmritikoshTests/ProgressiveAnswerMigrationTests",
    "KalsmritikoshTests/TypedFieldMigrationTests",
    "KalsmritikoshTests/JobObjectiveMigrationTests",
    "KalsmritikoshTests/WorkbenchDatasetMigrationTests",
    "KalsmritikoshTests/WorkbenchTransformMigrationTests",
    "KalsmritikoshTests/WorkbenchScenarioMigrationTests",
    "KalsmritikoshTests/ShellSessionMigrationTests",
    "KalsmritikoshTests/InvestigationCaseMigrationTests",
    "KalsmritikoshTests/InvestigationScopeArtifactMigrationTests",
    "KalsmritikoshTests/InvestigationSubjectIdentityMigrationTests",
    "KalsmritikoshTests/InvestigationAnalysisMigrationTests",
    "KalsmritikoshTests/InvestigationDeskReviewMigrationTests"
  ],
  "requiredSuites": [
    "MigrationMatrixTests",
    "MigrationFaultInjectionTests",
    "RealArchiveMigrationTests",
    "EvidenceDimensionMigrationTests",
    "ProfessionalIssueMigrationTests",
    "ProfessionalTaskMigrationTests",
    "SensitiveScopeMigrationTests",
    "WorkProductRunMigrationTests",
    "EmailParticipantMigrationTests",
    "SourceReliabilityAssessmentMigrationTests",
    "WorkflowRunMigrationTests",
    "WorkflowProvenanceMigrationTests",
    "WorkflowAutomationExecutionMigrationTests",
    "MethodRunMigrationTests",
    "MethodLifecycleMigrationTests",
    "MethodLifecycleLedgerHardeningMigrationTests",
    "UniversalSourceIntakeMigrationTests",
    "SinglePathIntakeIntegrityMigrationTests",
    "ExactByteBindingMigrationTests",
    "SourceReadinessMigrationTests",
    "ExactVersionChunkMigrationTests",
    "ContainerManifestMigrationTests",
    "ProgressiveUpgradeMigrationTests",
    "ProgressiveAnswerMigrationTests",
    "TypedFieldMigrationTests",
    "JobObjectiveMigrationTests",
    "WorkbenchDatasetMigrationTests",
    "WorkbenchTransformMigrationTests",
    "WorkbenchScenarioMigrationTests",
    "ShellSessionMigrationTests",
    "InvestigationCaseMigrationTests",
    "InvestigationScopeArtifactMigrationTests",
    "InvestigationSubjectIdentityMigrationTests",
    "InvestigationAnalysisMigrationTests",
    "InvestigationDeskReviewMigrationTests"
  ],
  "minimumExecutedTests": 359,
  "note": "v99->v100 (INV-08 + INV-12 case-scoped review desk: one NEW table — investigation_desk_reviews, a thin human confirm/dismiss disposition that REFERENCES a shared canonical item by soft id [reliability -> source_version_id; contradiction/gap -> the item id] and forks none of the shared authorities, never mutating their global status. CHECK item_kind reliability/contradiction/gap, decision confirmed/dismissed, nonblank item_id/actor; UNIQUE(case_id,item_kind,item_id) [one disposition per item per case]; CASCADE from cases. NEW table so the self-heal column probe distinguishes v100. InvestigationDeskReviewMigrationTests, 8. Floor 359 = 351 + 8 (set from the hosted migration-group actual). PRIOR v98->v99 (INV-04..07 analytical spine: four NEW tables — investigation_hypotheses (leads/hypotheses; CHECK kind lead/hypothesis, status proposed/confirmed/rejected/dismissed, nonblank statement, revision>=1; origin_hypothesis_id self-FK SET NULL; CASCADE from cases) + investigation_hypothesis_evidence (for/against links citing exact source_version + knowledge_object; CHECK stance for/against; UNIQUE(hypothesis,stance,source_version,knowledge_object); CASCADE from hypotheses) + investigation_evidence_requests (missing-evidence requests; CHECK status open/confirmed/fulfilled/cancelled; hypothesis_id FK SET NULL; CASCADE from cases) + investigation_worksheet_cells (5W1H; CHECK dimension who/what/when/where/why/how, status unknown/answered, and the answered<->answer+evidence pairing so an unknown is never fabricated; UNIQUE(case,dimension); CASCADE from cases). Persona reasoning state referencing canonical evidence by id; forks no Claim/gap/contradiction authority. NEW tables so the self-heal column probe distinguishes v99. InvestigationAnalysisMigrationTests, 9. Floor 351 = 342 + 9 (set from the hosted migration-group actual). PRIOR v97->v98 (INV-02/03 subject + identity-decision ledger: two NEW tables — investigation_subjects (a case->canonical-entity subject with a proposed->confirmed identity decision: CHECK status vocab proposed/confirmed/rejected, confirmed<->confirmer+timestamp paired, revision>=1, nonblank label/actor, UNIQUE(case_id,canonical_entity_id), CASCADE from investigation_cases) + investigation_identity_decisions (the append-only, REVERSIBLE merge decision log: CHECK decision_kind vocab mergeProposed/mergeConfirmed/mergeRejected/mergeReversed, winner!=loser, sequence>=1, UNIQUE(case_id,sequence), CASCADE from cases, prior_decision_id self-FK SET NULL linking confirm->proposal and reverse->confirm). A subject/decision references canonical entities by soft id and forks no second entity/alias/merge authority. NEW tables so the self-heal column probe distinguishes v98. InvestigationSubjectIdentityMigrationTests, 9. Floor 342 = 333 + 9 (set from the hosted migration-group actual). PRIOR v96→v97 (INV-01-C4 canonical case-scope fingerprint / staleness ledger: ONE table investigation_scope_artifacts recording, per case-produced artifact (Ask answer / MethodRun / Workbench dataset / work product), the deterministic scope fingerprint + case revision it was produced under — so a later scope change marks CURRENT surfaces stale WITHOUT rewriting the historical artifact (UNIQUE(case_id, artifact_kind, artifact_id) → immutable rows). Fingerprint is a 64-hex SHA-256 from the ONE CaseScopeFingerprinter over case id + revision + sorted authorized source-version set (scope identity, not confidence); the table CHECK enforces the 64-hex shape + closed kind vocab + revision>=1 + non-blank artifact id. FK CASCADE from investigation_cases; canonical evidence untouched (soft artifact_id). NEW table so the self-heal column probe distinguishes v97. InvestigationScopeArtifactMigrationTests, 8. Floor 333 = 325 + 8 (set from the hosted migration-group actual, not the authored count). PRIOR v95→v96 (INV-01-A Investigator case intake & scope: three NEW tables — investigation_cases (the case header + scope/out-of-scope statements + time window + status open/scopeConfirmed/closed + a SOFT confirmed_deadline_id referencing a CONFIRMED `deadlines` row only + advisory possible_deadline_note + revision CAS + actor) + investigation_case_sources (the in-scope/out-of-scope source dispositions — source_ref soft-references a canonical logicalSource/sourceVersion/workspaceSource; in_scope 0/1; UNIQUE(case_id, source_ref) is the HARD evidence boundary, one disposition per source; canonical source rows are never cascade-mutated) + investigation_case_events (append-only audit; closed action vocab created/scopeSet/sourceIncluded/sourceExcluded/scopeConfirmed/deadlineBound/reopened; UNIQUE(case_id, sequence)). A case is a LENS over the one engine: it references canonical workspaces/sources/deadlines by id and forks no canonical evidence/task/deadline/SensitiveScope authority. Truth boundaries: available-in-workspace ≠ authorized-for-this-case (only in-scope sources are authorized), possible deadline ≠ confirmed deadline (only a row in `deadlines` may bind). Ownership edges hard FK + CASCADE (workspace→cases→sources/events). NEW tables so the self-heal column probe distinguishes v96. Legacy pre-v96 data preserved with no fabricated cases. InvestigationCaseMigrationTests, 10. Floor 325 = 315 + 10 (set from the hosted migration-group actual, not the authored count — the recurring floor-from-actual lesson). PRIOR v94→v95 (SHELL-001 shared-shell navigation session: two NEW tables — app_navigation_sessions (one per scope_key, with the Back/Forward cursor current_index and a monotone revision) + app_navigation_entries (the ordered location stack: closed 12-destination vocab + optional context kind/id, UNIQUE(session_id,ordinal), CASCADE on session delete). Browser-style Back/Forward LOCATION history, autosaved so a relaunch resumes at the exact place — DISTINCT from workflow Prev/Next. NEW tables so the self-heal column probe distinguishes v95. ShellSessionMigrationTests, 10. Floor 315 = 305 + 10 (set from the hosted migration-group actual, not the authored count — the recurring floor-from-actual lesson). PRIOR v93→v94 (LAB-003 scenario overlays: four NEW tables — workbench_scenarios (base_dataset_revision + undo/redo pointer current_op_seq + status active/discarded/promoted + revision CAS) + workbench_scenario_operations (append-only monotone log, closed kind {valueOverride,proposedCorrection,classification,annotation,derivedExperimentalValue,rowInclusion,rowExclusion} + target_kind {cell,row} + status {live,abandoned}; a cell op requires a field; UNIQUE(scenario_id,sequence)) + workbench_scenario_reviews (reviewed-promotion routing: closed destination {userCorrection,workingFinding,methodRunInput,claimReview,workProductInput} + decision {accepted,rejected} + resulting_reference) + workbench_scenario_events (append-only audit, closed action vocab, UNIQUE(scenario_id,sequence)). A scenario is a NON-DESTRUCTIVE overlay: current state is REPLAYED from the live operations up to the pointer, undo/redo move the pointer, a new op after undo ABANDONS the redo branch (retained, never re-applied); canonical evidence + source cells + LAB-002 derivations are never mutated; promotion only RECORDS a human-reviewed routing to an existing authority. Ownership edges hard FK + CASCADE (dataset→scenarios→operations/reviews/events; operation→reviews). NEW tables so the self-heal column probe distinguishes v94. WorkbenchScenarioMigrationTests, 14. Floor 305 = 291 + 14. PRIOR v92→v93 (LAB-002 safe transformation engine lineage: three NEW tables — workbench_transformations (append-only, per-dataset monotone sequence, closed kind vocab {calculatedColumn,runningTotal,filter,sort,deduplicate,aggregate,pivot,join,rollingCalculation}, engine_version, spec_json, optional target_field_id + result_json) + workbench_derivations (one per derived value: optional output_cell_id / result_key / output_value) + workbench_derivation_inputs (the EXACT input cells a derived value read; UNIQUE(derivation_id,ordinal)). Also rebuilds workbench_dataset_events to widen the action CHECK with 'transformed', copying existing rows verbatim (no history invented/dropped). Ownership edges are hard FK + CASCADE (dataset→transformations→derivations→inputs; input_cell/output_cell/target_field → workbench_cells/_fields). NEW tables so the self-heal column probe distinguishes v93 (no stored-SQL sentinel needed for the co-migrated events CHECK rebuild). WorkbenchTransformMigrationTests, 14. Floor 291 = 277 + 14. PRIOR v91→v92 (LAB-001 canonical Workbench dataset model: seven NEW tables — workbench_datasets + _fields + _rows (stable row identity) + _cells (typed by a closed provenance kind {sourceValue,deterministicCalculation,userEntered,userCorrected,modelProposal,reviewed}) + _source_bindings (drill-through to the exact canonical origin: closed target_kind + target_id + optional source_version_id + locator_json) + _saved_views + append-only _dataset_events (revision history). The ONE canonical DataLab dataset authority; the earlier evidence_datasets/dataset_rows prototype is superseded and kept decode-only, so v91→v92 preserves it with NO fabricated Workbench datasets. Ownership edges are hard FK + CASCADE (workspace→dataset→fields/rows/cells/bindings/views/events; row→cells; cell→bindings); source_version_id is a soft reference (canonical rows never cascade-mutated by a dataset). CHECKs: nonblank title/field-name/target_id/actor/status, mode∈{simple,advanced}, revision/sequence>=1, one cell per (row,field), closed cell-kind + binding-target-kind + event-action vocab; per-dataset unique event sequence + unique field/row ordinal. NEW tables so the self-heal column probe distinguishes v92. WorkbenchDatasetMigrationTests, 14). Floor 277 = 263 + 14. PRIOR v90→v91 (TBJ-FINAL time-bounded job planning envelope: three NEW tables — job_objectives + job_plan_references + job_events. A Job is a DURABLE PLANNING ENVELOPE over existing task/deadline/workflow authorities, NOT a second task or deadline system: every plan reference points at a ProfessionalTask / WorkflowRun / step / requirement / evidence requirement / expected artifact that already exists in the canonical engine. Time-budget integrity: the only deadline a job may bind is a row in `deadlines` (the CONFIRMED table; candidates live in the separate `deadline_candidates` table), enforced at write time by JobRepository — a DeadlineCandidate can never become an authoritative budget. Deadline / workflow pointers are SOFT references (validated by the repository, the method_runs idiom), so no ON DELETE action can null a budget column out from under the basis-integrity CHECK; ownership edges (workspace, owning job) are hard FKs with CASCADE. CHECKs: nonblank title/actor/reference_id, budget-basis exclusivity (exactly the column matching the basis is populated; explicitDuration>0), closed lifecycle vocab {active,closed,abandoned} distinct from ProfessionalTaskStatus/WorkflowRunStatus, active⇔no closed_at, revision/sequence>=1, closed reference_kind {professionalTask,workflowRun,workflowStep,workflowRequirement,evidenceRequirement,expectedArtifact} + role + MAD flag; COALESCE-unique plan-reference dedup; per-job unique event sequence. NEW tables so the self-heal column probe distinguishes v91. Legacy pre-v91 data preserved with no fabricated jobs. JobObjectiveMigrationTests, 21). Floor 263 = 242 + 21. PRIOR v89→v90 (MMI-FINAL typed identity/document fields: NEW table typed_fields — a deterministic, block-backed typed field pinned to the EXACT evidence_block_id + source_version_id + locator it came from; FKs cascade from source_versions AND evidence_blocks; CHECKs on confidence[0..1], ocr_confidence null-or-[0..1], nonblank field_type + raw_value; the FIRST accepted producer for the USF-004 typedFields surface + the typedFieldExtraction readiness dimension; NEW table so the self-heal column probe distinguishes v90. A typed field is NOT a Claim. Legacy pre-v90 data preserved with no fabricated fields. TypedFieldMigrationTests, 15). Floor 242 = 228 + 14. PRIOR v88→v89 (AEE-M2 progressive answer revision ledger: EXTENDS the existing answer-ledger authority [answers/answer_claims/claim_evidence, v28] with two NEW tables — answer_revisions [immutable per-answer revision chain; UNIQUE(answer_id,revision_number); SHA-256-shaped content_hash CHECK; a correction needs a prior revision + nonblank reason and, via the (correction_of_revision_id, answer_id)→answer_revisions(id,answer_id) composite FK, can only reference a revision of the SAME answer; UNIQUE(id,answer_id) supports that FK] + answer_revision_events [append-only lifecycle ledger, UNIQUE(answer_id,sequence), closed 7-state vocab immediateFinding/groundedWorkingResult/analysisProgress/reviewReady/verifiedFinal/corrected/incomplete, content-bearing-requires-revision CHECK]; plus a nullable revision_id on answer_claims and nullable compat columns on answers [request_id/mission_lane/mission_objective/mission_deliverable/is_terminal/updated_at]. NEW tables + NEW columns so the self-heal column probe distinguishes v89. Legacy pre-v89 answers are preserved verbatim with NO fabricated revision history [no backfill]. ProgressiveAnswerMigrationTests, 15). Floor 228 = 214 + 14 [mandated]. PRIOR v87→v88 (USF-M3 progressive upgrade ledger: enrichment_jobs REBUILT into an exact-SourceVersion background-work queue [scope_kind legacySubject|sourceVersion; sourceVersion jobs carry a non-null source_version_id pinned by FK to source_versions; closed state pending/running/done/failed/blocked/cancelled/superseded; running-requires-lease CHECK; priority + not_before + lease columns; partial UNIQUE idempotency for one active job per (source_version_id,kind,producer_id,producer_version) and preserved legacy (subject_id,kind)] + append-only enrichment_job_events [closed action vocab, UNIQUE(job_id,sequence), CASCADE]; legacy rows preserved verbatim as legacySubject with NULL source_version_id [never guessed]; NEW columns/table so the self-heal column probe distinguishes v88; a job describes WORK, never readiness. ProgressiveUpgradeMigrationTests, 13). Floor 214 = 201 + 13. PRIOR v86→v87 (USF-M2 container coverage projection: container_manifests [one current inspection manifest per container SourceVersion; closed status complete/partial/blocked/unsupported/failed; count-consistency CHECK admitted+blocked+unsupported+failed <= regular_file_entries] + container_members [every member disposition incl. never-admitted; closed disposition vocabulary; admitted requires child+hash pinned to source_versions(id,content_hash); non-admitted forbids a fabricated child; UNIQUE(parent,ordinal), duplicate paths allowed]; NEW tables so the self-heal column probe distinguishes v87; processing projections only — source_versions/source_version_relations/source_readiness_* stay authoritative. ContainerManifestMigrationTests, 14). Floor 201 = 187 + 14. PRIOR v85→v86 (USF-002.1 exact-version chunk ownership: chunks.source_version_id soft column + index; v85→v86 backfill sets it ONLY where provable via chunk.evidence_block_id → block.source_version_id, legacy stays NULL; new column so the self-heal probe distinguishes v86. ExactVersionChunkMigrationTests, 9). Floor 187 = 178 + 9. PRIOR v84→v85 (USF-002 independent source readiness dimensions: source_readiness_aggregates + source_readiness_dimensions [ten closed dims per source version] + append-only source_readiness_events; conservative v84→v85 backfill from canonical evidence [preservation/metadata/text/structural/media/OCR]; CHECK/FK constraints; new tables so the self-heal column probe distinguishes v85. SourceReadinessMigrationTests, 17). Floor 178 = 161 + 17. PRIOR v83→v84 (USF-001.2 exact-byte binding: source_versions rebuilt so a non-legacy custody hash must be HEXADECIMAL — `content_hash NOT GLOB '*[^0-9a-f]*'` added to the SHA-256 branch, closing the v83 gap that accepted any 64-char lowercase string; the migration demotes any existing non-hex custody row to legacyImported; CHECK-only so the self-heal sentinel gains a v84 GLOB CHECK-text probe. ExactByteBindingMigrationTests, 9). Floor 161 = 152 + 9. PRIOR v82→v83 (USF-001.1 single-path intake integrity: source_versions non-legacy SHA-256 + same-logical supersedes composite FK + UNIQUE(id,logical_source_id)/(id,content_hash); source_intake_receipts composite FKs pinning receipt to the version's logical source + hash; ingest_file_attempts both-or-neither + composite FK. SinglePathIntakeIntegrityMigrationTests, 11). Floor 152 = 141 + 11. PRIOR v81→v82 (USF-001 universal safe intake: source_versions rebuilt with intake custody metadata + one-current partial UNIQUE index + one-current legacy repair; source_intake_receipts audit ledger + source_version_relations + version-linked ingest_file_attempts; NOT-NULL custody columns carry DEFAULTs so legacy short-form inserts round-trip. UniversalSourceIntakeMigrationTests, 14). Floor 141 = 127 + 14. PRIOR v80→v81 (PM-004.1 Stage 4 lifecycle ledger hardening: rebuilds method_runs to add the REVERSE supersession CHECK, method_reviews to add non-blank review_key / non-negative reviewed_content_revision / not-both-node-and-finding CHECKs, and method_validation_results to add non-blank validation_batch_id / non-negative evaluated_content_revision CHECKs — NO new column, every legacy value satisfies the new CHECKs. The migration pass is now atomic: a single OUTER savepoint wraps every pending migration AND the final PRAGMA foreign_key_check, so a post-pass FK violation rolls the whole pass + user_version back. The self-heal sentinel gains v81 CHECK-text probes. MethodLifecycleLedgerHardeningMigrationTests, 9). Floor 127 = 118 + 9. PRIOR v79→v80 (PM-004 Stage 4 generic method lifecycle: method_runs rebuilt to add content_revision + the 'paused' status and both-way completed/superseded CHECKs; method_evidence_links.input_role + COALESCE partial-unique indexes; method_reviews.review_key + reviewed_content_revision; method_validation_results.validation_batch_id + evaluated_content_revision; new method_run_events durable audit table with human-identifier + contiguous-sequence CHECKs; self-heal marker; SAVEPOINT rollback. The rebuild runs with foreign-key enforcement disabled for the whole migration pass — the SQLite-recommended pattern so DROP TABLE method_runs does NOT cascade-delete its children — followed by PRAGMA foreign_key_check; MethodLifecycleMigrationTests, 13). Floor 118 = 105 + 13. PRIOR v78→v79 (PM-002 Stage 4 method run-state ledger: method_runs + 7 child tables — nodes/edges/evidence_links/assumptions/findings/reviews/validation_results, composite (id, method_run_id) ownership FKs, partial-unique evidence-link indexes, human-only review CHECK, non-run-validation-subject-requires-id CHECK, workspace CASCADE, soft workflow references, self-heal marker, SAVEPOINT rollback; MethodRunMigrationTests). Floor 105 = 90 + 15 (MethodRunMigrationTests). Milestone migration matrix (v0/1/36/54/61..77 → v77), SAVEPOINT fault atomicity, genuine DDL/backfill/SQLITE_FULL failures, malformed partial schemas, synthetic real-archive preservation, the existing genuine v61→v62 preservation + rollback/retry, plus the OPS/PJE engine migrations through v75 (Issue/Task/SensitiveScope/WorkProductRun/email/reliability/workflow-run ledgers), v75→v76 (PJE-006B.1 workflow_step_runs.state_hash_semantics), and v76→v77 (PJE-007 provenance bridge: workflow_provenance_snapshots / workflow_provenance_references / workflow_attachment_bindings + provenance_semantics columns on step/artifact/decision, owner-exclusivity CHECK, partial UNIQUE owner indexes, CASCADE delete, canonical isolation, self-heal marker, SAVEPOINT rollback) suites. Counts by xcresulttool (parameterized function = 1): milestoneMigratesToLatest and faultAtBoundaryRollsBack each count as 1. Does NOT require the private owner archive (RealArchiveMigrationTests uses a synthetic on-disk archive; the sanitized owner archive is an owner step, ci/migrations/verify-real-archive.sh). v77→v78 (PJE-010 automation execution ledger: workflow_automation_executions table + UNIQUE idempotency index + audit indexes, CASCADE from workflow_runs, status CHECK, canonical isolation, self-heal marker, SAVEPOINT rollback). Floor 90 = 80 (PJE-009 group count) + 10 (WorkflowAutomationExecutionMigrationTests)."
}