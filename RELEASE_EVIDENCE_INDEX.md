# RELEASE_EVIDENCE_INDEX

**Status: CURRENT.** Created 2026-07-24 (MASTER-001). One index for every release gate and the
evidence that proves it. A gate is **PASS** only when it names concrete, reproducible evidence
(a green CI run URL/id, a test identifier, a recorded owner-hardware run, a signed-build log).
No gate is PASS on assertion alone. Authority: `SHIP_DECISIONS.md` §3 + this file, under
`WHOLE_PROJECT_COMPLETION_PROGRAM.md`.

Evidence states: `PENDING` · `IMPLEMENTED` · `UNIT` · `INTEGRATION` · `REAL_DATA` · `RELEASE` ·
`PASS`.

## A. Functional gates
| Gate | Requirement | Evidence | State |
|---|---|---|---|
| F1 | All advertised formats pass a declared matrix | Every advertised structural format has a dedicated fixture suite (DocxParserTests + MarkdownParserTests added; ZIP proven via ContainerSafety/ContainerIngestIntegration inside the gate); parser-fixtures named check floor 126, hosted-green PR #73 run 31170924940; SUPPORTED_FORMATS_V1.md synced to code truth | INTEGRATION |
| F2 | All five persona primary workflows complete | PersonaJobMatrixCoverageTests: 71 launchable jobs pinned (16/14/14/13/14) against the 75-row coverage matrix (read in-test); live routing (intake->real case->findings/closure/custody/contradictionGap/dataLab) proven for ALL five personas + fail-closed for unwired kinds; hosted-green PR #73 run 31170924940. Owner acceptance journeys (section E) remain the owner-run half of this gate | INTEGRATION |
| F3 | All material Claims reopen exact evidence | CitationResolver authority union (chunk/event/relationship/deterministic-evaluation/authority layers + ledger existence probes) wired into EvidenceVerifier + AppState; `CitationResolverTests` (11) + `EvidenceVerifierCitationIntegrationTests` (4) hosted-green on PR #71 run 31168181813 (build-and-test total=3369, 0 failed); reopen path unchanged (PJE009CitationTamperTests, GenericFactHistoryCitationTests) | INTEGRATION |
| F4 | Report and receipt match | report/receipt identity tests (per template) | INTEGRATION |
| F5 | Previous work-product runs reopen | OPS-004 reopen tests green (run 30271215068, SHA 8678c0c) | INTEGRATION |
| F6 | Workflow runs resume after relaunch | Stage 3 gate PROVEN — `PJE011CompleteSyntheticWorkflowTests.completeWorkflowWithRelaunch` (start→save→close→resume→complete method→human decision→cited work product→reopen exact run) + PJE-012 final acceptance; see `STAGE3_ACCEPTANCE.md` | PASS |

## B. Truth gates (all must be zero / always)
| Gate | Requirement | Evidence | State |
|---|---|---|---|
| T1 | Persona truth divergence = zero | CrossPersonaTruthInvarianceTests (same destinations all personas/modes; PersonaRoutingContext + PersonaPolicy pinned to exact presentation/default field sets; shared builder+policy persona-blind) + ci/guards/persona-neutral-truth.sh (evidence layers may never reference persona/shell types) + Claim personaInvariance (structural); hosted-green PR #74 run 31172818544 | INTEGRATION |
| T2 | Unsupported material Claims exported = zero | fail-closed validator tests | INTEGRATION |
| T3 | Duplicate evidence counted as independent corroboration = zero | DuplicateSourceIndependenceTests through the SHARED AssertabilityContextBuilder (dup/forwarded copies collapse to one group; unkeyed/blank keys contribute nothing; pile + one independent = exactly two groups) + AssertabilityPolicyTests; production keys batch-resolved in HybridRetriever; hosted-green PR #74 run 31172818544 | INTEGRATION |
| T4 | Silent global fallback = zero | no-fallback assembly tests + arch guard | INTEGRATION |
| T5 | Confirmed professional decisions always have a human audit record | Stage 3 engine enforces human-only decision/approval (humanDecision/humanApproval require `actorKind==.human` + non-blank identity), persisted as a tamper-evident `WorkflowDecision` with provenance basis (WorkflowRunRepositoryTests, PJE006C decision/approval suites, PJE011 tamperDecisionBasisDetected) | INTEGRATION |

## C. Security gates
| Gate | Requirement | Evidence | State |
|---|---|---|---|
| S1 | No network provider available in Release | PrivacyGate + release-build check | IMPLEMENTED |
| S2 | Sensitive evidence never leaks across scope | SensitiveScope ledger + authority hardening DONE (OPS-003A/A.1/A.2 — schema v71, lineage-aware effectiveLabel, brokenLineage denial, userConfirmed authority, 7th Claim branch, File→KO legacy sync, 32 SSA tests); OPS-003B retrieval+prompt enforcement DONE (SensitiveRetrievalPolicy fail-closed + WalkStep evidence-KO filtering; HybridRetriever scoped path sole authority; raw PromptTemplates overloads private; PromptContextAuthorizer async revalidation; retrieveAuthorized()+promptAuthorizer on all 8 experts; mandatory non-optional access: on answerStream; 34 unit tests); OPS-003C report/receipt/export enforcement DONE (scopeFilter() in WorkProductAssemblyService.composeThroughRegistry(); 5 WorkProductScopeEnforcementTests; SHA 33dc042, run 30221075298, floor 931); OPS-003D.1.1 screen enforcement DONE (ScreenScopeAuthorizer fail-closed; SourceViewer/EvidenceViewer/EventDetailSheet all gated; mandatory koID; event-level auth before load(); AuthorizationTaskID live revalidation; isTestSentinel #if DEBUG; .workspace removed; 10 ScreenScopeEnforcementTests; SHA fa7f2d2, run 30257431207, floor 945); OPS-003D.1.2.1 mutation-service actor + cumulative tests DONE (SensitiveScopeMutationService wraps all assign/revoke; policyChanges AsyncStream; authorized=nil pending state in SourceViewer+EvidenceViewer; EventDetailSheet revision-aware .task; fifth arch guard syntax-resilient; 22 cumulative ScreenScopeEnforcementTests; SHA 80c07d7, run 30261625706, floor 957); CI-001C cross-surface E2E DONE (10 SensitiveScopeEndToEndTests: all-surfaces deny, no leakage, brokenLineage, cross-workspace, prompt-time revalidation, mutation-service cycle, file inheritance, persona invariance, manifest/receipt, nil-repo fail-closed; 6-job hosted-green run 30264550724 SHA 68f3759, total=967/sensitive-export=122, all 7 suites ran) | PASS |
| S3 | Text + visual redaction verified | GENERATED-export redaction proven at output-representation level: ReleaseSecurityGateTests (all-format bytes incl. STORE-mode DOCX/XLSX, PDFKit page-text extraction of rendered PDF, combined email/phone/custom channels, citation-leak fail-closed) + RedactionVerifierTests + ExportEngineTests fail-closed; hosted-green PR #72 run 31169153324. Source-document VISUAL/binary redaction is NOT a v1 capability (RED-002) and is not claimed anywhere (audited) | INTEGRATION |
| S4 | Temporary exports cleaned safely | Exports render in memory + atomic write (no staging dir by design); ReleaseSecurityGateTests.failedWriteLeavesNoOrphan + ContainerIngestIntegrationTests.extractionTempDirCleanedUp (usfm2-expand root gone after expand() on success AND failed-member paths); hosted-green PR #72 run 31169153324 | INTEGRATION |
| S5 | Prompt injection cannot alter system authority | PromptInjectionGuard tests | IMPLEMENTED |
| S6 | Archive traversal + malformed-file tests pass | ContainerSafetyTests + ContainerIngestIntegrationTests: traversal/absolute/drive/NUL paths, symlink-escape containment, depth/entry/nested-count/root-member/root-byte budgets, 200:1 ratio bombs, encrypted + corrupt + unsupported members visible, cycles, zero-byte legality, honest RAR/7z unsupported manifest; hosted-green PR #72 run 31169153324 (also enforced in the parser-fixtures gate from macro E) | INTEGRATION |

## D. Scale gate
| Gate | Requirement | Evidence | State |
|---|---|---|---|
| SC1 | Market only the largest corpus actually tested on owner hardware | recorded owner-hardware run (GB figure) | PENDING |
| SC2 | No unverified 100 GB / 1 TB claim in any copy | copy audit vs SHIP_DECISIONS | PENDING |

## E. Owner acceptance (per persona: Investigator, Researcher, Journalist, Individual, Lawyer)
For each persona, record a dated run:
```
create workspace → add real sources → complete primary workflow → inspect evidence
→ create work product → seal receipt → quit and reopen → resume/reopen everything
```
| Persona | Evidence (dated owner run) | State |
|---|---|---|
| Investigator | (pending) | PENDING |
| Researcher/Historian | (pending) | PENDING |
| Journalist | (pending) | PENDING |
| Individual | (pending) | PENDING |
| Lawyer | (pending) | PENDING |

## F. App Store gates
| Gate | Requirement | Evidence | State |
|---|---|---|---|
| AS1 | Signed archive | (archive log) | PENDING |
| AS2 | Clean Mac installation | (clean-machine run) | PENDING |
| AS3 | Offline workflow | (offline run) | PENDING |
| AS4 | Privacy manifest | (file) | PENDING |
| AS5 | Privacy policy | (draft/host) | PENDING |
| AS6 | Terms / EULA | (draft/host) | PENDING |
| AS7 | Support page | (draft/host) | PENDING |
| AS8 | Screenshots without personal data | (assets) | PENDING |
| AS9 | Limitations disclosed | (copy) | PENDING |
| AS10 | Early-access wording | (copy) | PENDING |
| AS11 | Final capability matrix | SUPPORTED_FORMATS / matrix | PENDING |

## G. CI evidence (CI-001A)
| Item | Evidence | State |
|---|---|---|
| Named GitHub checks on HEAD | `build-and-test` + `architecture-guards` (workflow "Build + Guards") | PASS |
| First green hosted run | run **30107577371**, conclusion **success**, SHA `fbd124358f5cf01583800b91a4a6ac1486e0eb1a`, 2026-07-24 | PASS |
| Full test target executed (from .xcresult) | verifier: `result=Passed total=781 passed=781 failed=0 (floor=781)` | PASS |
| Runner / toolchain | macOS 26.4, Xcode 26.6; test run `MACOSX_DEPLOYMENT_TARGET=26.4` (labelled compatibility override, within macOS 26 floor — NOT proof the pinned 26.5 point release passed) | RECORDED |
| Architecture guards green | `ci/guards/run-all.sh` on ubuntu-latest | PASS |
| Latest green hosted run | run **30140696084**, success, SHA `b0f33428332ff41cf5b88bd2bcd6fd3b55fe1f4a` (MIG-001B), verifier `total=794 failed=0 (floor=794)` | PASS |
| Migration matrix (MIG-001A) | 10 milestone fixtures → v67, preservation + integrity + fk, reopen | Integration verified |
| Migration fault atomicity (MIG-001B) | boundary faults A/B/E/F, genuine DDL/backfill/SQLITE_FULL failures, malformed partial schemas fail-closed | Integration verified |
| Real-archive migration | synthetic v66→v67 archive, correct hash semantics + preservation | Integration verified (synthetic) |
| Sanitized REAL owner archive — CAPABILITY | MIG-001C: `OwnerArchiveMigrationAcceptanceTests` + manifest schema + rewritten `verify-real-archive.sh` run the REAL migration path on an external archive; end-to-end self-test PASSED on a synthetic owner-like archive (report: `ci/migrations/fixtures/synthetic-selftest-001-acceptance-report.json`) | Integration verified |
| Sanitized REAL owner archive — RUN | owner supplies sanitized archive + manifest per `ci/migrations/real-archive-manifest.schema.json`; then `ci/migrations/verify-real-archive.sh <archive> <manifest>` | PENDING (owner run) |
| **CI-001B five named checks (all green)** | run **30144232341**, success, SHA `806325933ba9c7034488f80cd9ee1bcb23d1d6d2`, 2026-07-25: `architecture-guards` ✓ · `build-and-test` ✓ (794 executed, 0 failed, floor 794) · `migration-matrix` ✓ (23 tests, 4 suites) · `parser-fixtures` ✓ (78 tests, 20 suites) · `report-receipt-integrity` ✓ (68 tests, 6 suites). Group manifests: `ci/test-groups/*.json` @ this SHA. | PASS |
| parser-fixtures status rule | Green proves the CURRENTLY REGISTERED fixtures only — release gate F1 (complete advertised-format matrix) remains PENDING | RECORDED |
| sensitive-export job | CI-001C: SHA `68f3759`. Hosted run **30264550724**, success, 2026-07-27: sensitive-export total=122 passed=122 failed=0 result=Passed floor=122 ranSuites=7; full-suite build-and-test total=967 passed=965 failed=0 skipped=2 (floor=967). All 6 checks green. | PASS |
| OPS-001 Issue Engine (schema v68) hosted-green | run **30148706330**, success, SHA `1673900ffb04907ac26130a4400794e61cf4f8a9`, 2026-07-25: build-and-test 810 total / 808 passed / 2 skipped (env-gated) / 0 failed, floor 810; migration-matrix 23/4 suites; parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓ | PASS |
| OPS-002 Task/Deadline Engine (schema v69) hosted-green | run **30156183281**, success, SHA `1f7bd0a2cc03edd342ec447468557e78ab7eed32`, 2026-07-25: build-and-test 837 total / 835 passed / 2 skipped (env-gated) / 0 failed, floor 837 (raised 810 → 837: +27 OPS-002 tests); migration-matrix 31/6 suites (adds ProfessionalIssueMigrationTests + ProfessionalTaskMigrationTests, floor 23 → 31); parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Truth rule `DeadlineCandidate ≠ Deadline` enforced in SQL (`UNIQUE(source_candidate_id)`) and repository, proven by tests. CORRECTION 2026-07-25: this row was recorded before the reviewer's NO-GO; it proves the IMPLEMENTATION run only. Acceptance required OPS-002.1 — closed by the row below. | IMPLEMENTATION PASS — acceptance closed by OPS-002.1 |
| OPS-002.1 confirmation-provenance hardening (schema v70) hosted-green | run **30160395578**, success, SHA `b84b11f9b8d6d26499764ceedc01728919a20421`, 2026-07-25: build-and-test 842 total / 840 passed / 2 skipped (env-gated) / 0 failed, floor 842 (raised 837 → 842: +4 ProfessionalTaskProvenanceTests, +1 v69→v70 migration test); migration-matrix 32/6 suites (floor 31 → 32); parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Corrections proven: rule confirmation requires candidate-scoped `deadlineBasis` links resolving to EXACT evidence (EvidenceBlock+source version, or Claim with exact evidence ref) — entity/context/task-scoped/KO-only links refused; cross-workspace primary Issue rejected (`crossWorkspacePrimaryIssue`); confirmed Deadlines only on open/inProgress/blocked tasks (`taskNotOperational`); rule ID + version persisted on `professional_task_reviews` (v70, NULL for pre-v70 rows, survives reopen). | PASS — acceptance still held pending OPS-002.2 |
| OPS-002.2 exact evidence binding + race-safe confirmation hosted-green | run **30182173679**, success, SHA `58e9f36a963b3e799018d89e895c49f8f6f2fafc`, 2026-07-26: build-and-test 845 total / 843 passed / 2 skipped (env-gated) / 0 failed, floor 845 (raised 842 → 845: +3 ProfessionalTaskProvenanceTests — mismatchedEvidencePairRefused, staleValidationCannotCreateDeadline, concurrentConfirmationsYieldOneDeadline); migration-matrix 32/6 suites; parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Schema unchanged (v70). Corrections proven: Claim evidence path now binds block and source version as a matched pair (`JOIN source_versions sv ON sv.id = b.source_version_id AND sv.id = r.source_version_id`) — block from version A with source_version B refused; `Database.withSavepoint` synchronous isolated closure makes all validation (candidate pending, task operational, precision, exact evidence, no existing Deadline) non-interleavable — validate-then-write race eliminated. Correction history: `04388c7` closed the original four-gap review. OPS-002.2 closed the remaining evidence-pair and confirmation-race findings. | PASS — closes OPS-002 final acceptance |
| OPS-003A SensitiveScope protection ledger (schema v71) hosted-green | run **30184159312**, success, SHA `44c4b3d`, 2026-07-26: all five checks green, xcresulttool floor 857 (raised 845 → 857: +12 SensitiveScopeRepositoryTests); migration-matrix 37/7 suites (adds SensitiveScopeMigrationTests); parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Schema v71: `sensitive_scope_assignments` + `sensitive_scope_reviews` (append-only; SAVEPOINT-atomic assign/revoke); `SensitivityInheritance.inherit(from:)` (max sensitivity, sticky privilege), `SensitivityInheritance.canRelease(_:at:)`; base CRUD `SensitiveScopeRepository`. NOTE: implementation run only — acceptance required A.1 + A.2, closed by the rows below. | IMPLEMENTATION PASS — acceptance closed by OPS-003A.2 |
| OPS-003A.1 protection authority lineage + legacy compatibility hardening hosted-green | run **30193365206**, success, SHA `1720bae`, 2026-07-26: build-and-test 891 IDE / 875 xcresulttool total / 889 passed / 2 skipped (env-gated) / 0 failed, floor 857 → 875 (+18 new tests: SensitiveScopeRepositoryTests 7 → 25); migration-matrix 37/7 suites; parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Schema v71 unchanged. Delivers 10 of 13 reviewer requirements: `SensitiveScopeTarget` (kind+id hashable key), `AssignmentAuthority` enum, target-existence validation in savepoint, lineage-aware `effectiveLabel` (8 kinds), `ProtectionResolution.brokenLineage`, legacy `knowledge_objects.privileged` sync (KO targets only), batch keyed by `SensitiveScopeTarget`, `reviews(forAssignmentID:)`, throwing `decodeAssignment`. NOTE: three gaps identified by reviewer in follow-on NO-GO — closed by OPS-003A.2. | HARDENING PASS — acceptance closed by OPS-003A.2 |
| OPS-003A.2 human-authority + Claim-lineage + File-legacy-sync hardening hosted-green | run **30194470481**, success, SHA `1fb9c7c`, 2026-07-26: build-and-test 882 xcresulttool total / 880 passed / 2 skipped (env-gated) / 0 failed, floor 875 → 882 (+7 tests: SensitiveScopeRepositoryTests 25 → 32); migration-matrix 37/7 suites; parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Schema v71 unchanged. Closes all three reviewer gaps over A.1: (1) `userDirect` renamed to `userConfirmed(actorID:confirmationID:privileged:)` — actorID stored as `assigned_by`, confirmationID embedded in origin as `"user_confirmed:<UUID>"`, whitespace-only actors rejected after trim in assign+revoke; (2) Claim lineage 7th UNION ALL branch: Claim→EB(`claim_evidence_ref.evidence_block_id`)→`evidence_block_objects`→KO→File — proven by `lineage_claimViaEBThroughEBOToProtectedFile` (ref row carries both `knowledge_object_id`=public KO and `evidence_block_id`=EB linking to restricted KO's file); (3) `assign()` propagates `privileged=1` to ALL child KOs when target is File+privileged; `revoke()` checks each child KO and clears `privileged=0` only when no remaining direct-KO or active-file privileged assignment covers it. `1fb9c7c` descends from `44c4b3d` and `1720bae` — this single hosted-green run proves the combined code state. | PASS — closes OPS-003A combined acceptance |
| OPS-003B retrieval+prompt enforcement — all 10 reviewer corrections applied (schema v71 unchanged) | Two-pass correction of reviewer NO-GO on local `717ca27`; floor raised 882 → 926 (+34 original tests → 916; +10 second-pass tests → 926; SensitiveRetrievalEnforcementTests 18 → 27; SensitivePromptEnforcementTests 16 → 17). All 10 required corrections: (1) `globalPermissive()` removed from `SensitiveScope`; `testUnrestricted(purpose:)` is `#if DEBUG`-only with sentinel UUID; all production callers use `testUnrestricted()` in DEBUG; (2) raw `RetrievalResult` PromptTemplates overloads are now `private` (not merely internal) — compile-time enforcement proven by updated test 13 + arch guard test 17 (`unscopedRetrievalThrowsWhenNoAccessContextPresent`); (3) pre-enrichment entity filter in `HybridRetriever` before authority density seeding loop; pre-enrichment authority KO filter before chunk injection; blocked entities/KOs cannot seed DocumentFitness or authority-chunk injection; (4) workspace enforcement: `SensitiveRetrievalPolicy` checks `workspaceRepository.koIDsInWorkspace` + `entityIDsInWorkspace` in step 3; fails closed on any repo error; sentinel scope skips enforcement; proven by 9 new workspace enforcement tests (cross-workspace chunk/event/entity denied, same-workspace allowed, multi-workspace source, repo-error fail-closed, sentinel bypass); (5) memory path: `phase1Instant` checks `memory.keyEventIDs` (empty → withheld), then filters events through policy — any blocked event withholds entire narrative; proven by `memoryPhase1WithheldWhenContributingEventNotInWorkspace`; (6) table fast path: `tableFastPath` always returns nil — cannot prove block→KO workspace membership; (7) all `MasterBrain` entry points (`answer`, `answerWithDiagnostics`, `computeVerified`, `tryReconstructHistoryStreaming`, `phase1Instant`, `tableFastPath`) require mandatory non-optional `access: SensitiveAccessContext`; corrective retrieval in `computeVerified` and `tryReconstructHistoryStreaming` both use scoped retrieve path; (8) `Expert.retrieveAuthorized()` throws `SensitiveRetrievalError.unscopedRetrieval` when `access == nil`; raw `PromptTemplates` overloads `private`; both enforced by compile time + arch guard test; (9) S2 remains UNIT until hosted-green; (10) amending `717ca27` to fold all corrections. SHAs: `4cee846` (all 10 corrections) + `d7a1570` (OPS-003B.1: walk-step batch + test seed fixes — see row below). | UNIT |
| OPS-003B.1 walk-step batch resolution + test seed correctness (schema v71 unchanged) | Two bugs found in hosted CI run 30219100957 (SHA 4cee846) by the first push: (1) `SensitiveRetrievalPolicy` step-1 batch collection omitted walk-step evidence KO IDs — `permitted()` returned nil→default for restricted KOs in walk steps, so `walkStepDeniedWhenEvidenceKOBlocked` passed the step when it should have been denied; fix: `for w in result.walkSteps { for id in w.evidenceObjectIDs { koIDs.insert(id) } }`. (2) Tests 2/13/16/17/18 used unseeded UUIDs expecting "default internalLevel" — but `effectiveLabel()` returns `.brokenLineage` for KOs not found in `knowledge_objects`, not `nil`; the `nil` case in `permitted()` is only reached when the KO wasn't included in the batch at all; fix: seed every "permitted" KO so `effectiveLabel` returns `.resolved(internalLevel)`. Hosted run **30219597170**, success, SHA `d7a1570752975ee3e74ae4148d86534ceb7b94c7`, 2026-07-27: build-and-test total=926 passed=924 skipped=2 failed=0 (floor=926); migration-matrix 37/7; parser-fixtures 78/20; report-receipt-integrity 68/6; guards ✓. Floor 926 unchanged (same 27 enforcement + 17 prompt tests; no new tests). S2 = UNIT (enforcement surfaces OPS-003C/D still PENDING). | UNIT — closes OPS-003B |
| OPS-003C report/receipt/export scope enforcement hosted-green | SHAs `6556768` (impl) + `33dc042` (baseline floor fix: 945→931, CI-measured). Hosted run **30221075298**, success, SHA `33dc042`, 2026-07-27: build-and-test total=931 passed=929 failed=0 skipped=2 (floor=931); migration-matrix ✓; parser-fixtures ✓; report-receipt-integrity ✓; architecture-guards ✓. Floor 926→931 (+5 WorkProductScopeEnforcementTests). NOTE: local count was 947 (16-test persistent local-vs-CI gap from prior sessions; floor tracks CI-measured xcresulttool totalTestCount). Baseline-fix run needed because first push set floor=945 (local count) before checking CI — lesson: always use prior CI count + new tests for the floor, not local count. S2 → INTEGRATION (OPS-003C done; OPS-003D screen enforcement still PENDING). | INTEGRATION — closes OPS-003C |
| OPS-003D screen enforcement hosted-green | SHA `3105de2`. Hosted run **30227400079**, success, 2026-07-27: build-and-test total=935 passed=933 failed=0 skipped=2 (floor=935); all five checks green. Floor 931→935 (+4 ScreenScopeEnforcementTests). Surfaces covered: SensitiveScope.screen() production factory (not #if DEBUG); AppState.sensitiveScopes wired from same DB; SearchView.screenFilter() filters FTS chunks by KO scope (fail-open when repo nil); SourcesView.screenFilter() filters recent KOs; AskView replaced testUnrestricted() with screen scope (compiles in release; privileged evidence excluded from global Q&A). S2 → IMPLEMENTED (all four surfaces: ledger + retrieval/prompt + report/receipt/export + screen). NOTE: reviewer issued a formal NO-GO on this run — reviewer corrections required via OPS-003C.2 + OPS-003D.1 before CI-001C. This row is superseded by the correction rows below. | SUPERSEDED — see correction rows |
| OPS-003C.2 reviewer corrections to compose() (mandatory access + 3 entry guards + scopeFilter reorder) hosted-green | SHA `3ac512d`. Hosted run **30229083972**, success, 2026-07-27: build-and-test total=938 passed=936 failed=0 skipped=2 (floor=938); architecture-guards ✓; parser-fixtures ✓; report-receipt-integrity ✓; migration-matrix ✓. Floor 935→938 (−1 removed `noAccessContextBypassesScopeFilter` + 4 added: `exportWithWrongPurposeDenied`, `exportWithMismatchedWorkspaceIDDenied`, `blockedClaimProducesEmptyManifestAndHashes`, `privilegedTextAbsentFromAllSections` = net +3, 8 WorkProductScopeEnforcementTests total). Corrections: `compose()` `access: SensitiveAccessContext` mandatory (non-optional); 3 entry guards (purpose must be `.export`, workspaceID must match workspace.id, sensitiveScopes must be non-nil); `scopeFilter` reordered (claims-empty check before repo-nil guard); sentinel-overloading bypass test removed; 11 test-file call sites updated with proper export access context. `registryFailureNeverFallsBack` wires real `SensitiveScopeRepository` (empty workspace → claims-empty returns early → registry throws `missingComposer`). S2 remains IMPLEMENTED pending OPS-003D.1. Reviewer-required follow-up OPS-003C.2.1 recorded in next row. | HOSTED-GREEN |
| OPS-003C.2.1 fail-closed on incomplete scope resolution + pin baseline SHA hosted-green | SHA `2b4b3e1`. Hosted run **30230056889**, success, 2026-07-27: build-and-test total=939 passed=937 failed=0 skipped=2 (floor=939); all five checks green. Floor 938→939 (+1 `missingResolutionFailClosedDeniesClaim`). Changes: `scopeFilter() case nil → return false` (was: `access.scope.permits(.internalLevel)` — fail-open default); `ci/test-baseline.json` baselineCommit pinned to exact SHA `3ac512d3449e474bba9e6051be207028312fef83` (not HEAD). Test proves: `batchResolution` returns `.brokenLineage` for an unknown KO (never nil), so the nil→false guard is the correct fail-closed backstop; regression guard verifies unassigned KO still passes with .restricted ceiling. S2 remains IMPLEMENTED pending OPS-003D.1 acceptance. | ACCEPTED — OPS-003C.2 + OPS-003C.2.1 combined |
| OPS-003D.1 screen gate rewrite: ScreenScopeAuthorizer + evidence viewers + fail-closed tests | SHA `2c52a87`. Hosted run **30254000425**, success, 2026-07-27: build-and-test total=945 passed=943 failed=0 skipped=2 (floor=945); all five checks green. Floor 939→945 (−4 mirrored-logic ScreenScopeEnforcementTests + 10 real-authorizer tests = net +6). Changes: (1) `ScreenScopeAuthorizer` actor added (fail-closed; batchResolution-direct; internal sentinel `00000001-...` not test sentinel); (2) old `SensitiveScope.screen()` removed; `globalOwnerRetrieval()` factory added; (3) `isGlobalOwnerBypass` added to `SensitiveScope`; `SensitiveRetrievalPolicy` skips workspace enforcement when set; (4) `AppState.screenAuthorizer` wired once; (5) `SearchView` + `SourcesView` use shared authorizer (removed fail-open private screenFilter); (6) `AskView` uses `globalOwnerRetrieval()`; (7) `SourceViewer` + `EvidenceViewer` gate on koID (optional); `EventDetailSheet` gates source-panel display; Reveal/Open/copy blocked when denied; (8) `FactStatusView` passes koID to SourceViewer. NOTE: reviewer issued formal NO-GO on semantic grounds after HOSTED-GREEN — six gaps identified (optional koID bypass, missing event-level gate, unimplemented .workspace boundary, isTestSentinel in production, no live revalidation, tests prove actor not UI paths). Corrected by OPS-003D.1.1 in next row. | HOSTED-GREEN — reviewer NO-GO, see OPS-003D.1.1 |
| OPS-003D.1.1 close viewer identity, event-detail and live-revalidation bypasses | SHA `fa7f2d2`. Hosted run **30257431207**, success, 2026-07-27: build-and-test total=945 passed=943 failed=0 skipped=2 (floor=945); all five checks green. Floor unchanged (945 − 10 + 10 = 945). Six NO-GO gaps closed: (1) `ScreenAccessBoundary.workspace(UUID)` removed — only `.globalOwner` exists until workspace membership enforcement is implemented; (2) `ScreenScopeAuthorizer.authorize(target:boundary:)` generic form added accepting any `SensitiveScopeTarget` kind; (3) `SensitiveScope.isTestSentinel` wrapped in `#if DEBUG … #else return false #endif` so test bypass UUID is inert in RELEASE builds; (4) `SourceViewer.koID` made mandatory non-optional (`public init(url: URL, koID: UUID, range: SourceRange? = nil)`); optional bypass branch removed from body; (5) `AuthorizationTaskID(targetID:policyRevision:): Equatable & Hashable` added; both `SourceViewer` and `EvidenceViewer` use it in `.task(id:)` so open viewers revalidate when policy changes; (6) `AppState.sensitiveScopeRevision: Int` + `notifyScopePolicyChanged()` added; (7) `EventDetailSheet` authorizes the event itself (`SensitiveScopeTarget(kind: .event, id: event.id)`) BEFORE calling `load()`; denied → `restrictedEventPlaceholder` shown, no repositories touched; allowed → `load()` proceeds, source panel separately gated; (8) 10 new `ScreenScopeEnforcementTests` proving all six fixes (mandatory koID, restricted KO blocks content, direct event SSA blocks sheet, source-KO inheritance blocks event, direct event SSA / source KO independently permitted, workspace boundary unavailable, isTestSentinel DEBUG-only, assignment added during view hides on revalidation × 2, revocation restores access). | HOSTED-GREEN — reviewer NO-GO, see OPS-003D.1.2 |
| OPS-003D.1.2 `SensitiveScopeMutationService` + pending-state + fifth arch guard | SHA `9a9a3eb`. Hosted run **30260059017**, success, 2026-07-27: build-and-test total=943 passed=941 failed=0 skipped=2 (floor=943); all five checks green. Floor regressed 945→943 (10 OPS-003D.1.1 tests replaced by 8 OPS-003D.1.2 tests — floor regression was a NO-GO finding). Three semantic gaps from D.1.1 NO-GO closed: (1) `SensitiveScopeMutationService` actor wraps all `SensitiveScopeRepository.assign()`/`revoke()` calls, increments `revisionCount`, yields on `nonisolated let policyChanges: AsyncStream<Void>` only on success; `AppState.mutationService` wired; `Task { @MainActor [weak self] in }` observes `policyChanges` and calls `notifyScopePolicyChanged()`; (2) `authorized = nil` before every `await authorizer.authorize(...)` in `SourceViewer` and `EvidenceViewer` — fail-closed pending state during revalidation; (3) `EventDetailSheet` `.task(id: AuthorizationTaskID(...))` revision-aware, sets `eventAuthorized = nil` first (shows ProgressView), clears all sensitive state on denial (`sourceObject`, `sourceURL`, `entities`, `outgoing`, `incoming`, `assertions`, `versionCount`, `sourceAuthorized`), resets `loading = true`; (4) `ci/guards/sensitive-scope-mutation-bypass.sh` (fifth guard) added to `ci/guards/run-all.sh`. Reviewer issued NO-GO on three counts: (1) guard used same-line-only patterns `\.assign(target:` / `\.revoke(assignmentID:` — bypassed by multiline Swift call forms; (2) 10 OPS-003D.1.1 tests deleted (floor regressed) — security corrections must be cumulative; (3) no test proving `policyChanges` stream yield or AppState observer received signals. Corrected by OPS-003D.1.2.1. | HOSTED-GREEN — reviewer NO-GO, see OPS-003D.1.2.1 |
| OPS-003D.1.2.1 harden mutation guard and restore cumulative security tests | SHA `80c07d7`. Hosted run **30261625706**, success, 2026-07-27: build-and-test total=957 passed=955 failed=0 skipped=2 (floor=957); all five checks green. Floor 943→957 (+14: 10 OPS-003D.1.1 tests restored + all 8 OPS-003D.1.2 tests kept + 4 `policyChanges` stream integration tests added = 22 `ScreenScopeEnforcementTests` cumulative). Three NO-GO corrections: (1) guard rewritten with `'\.(assign|revoke)[[:space:]]*\('` — syntax-resilient, catches same-line and multiline Swift call forms (e.g. `repository.assign(\n    target: target,` correctly detected); (2) all 10 OPS-003D.1.1 tests restored; all 8 OPS-003D.1.2 tests kept; security tests are additive, never exchanged; (3) 4 integration tests added using `private actor SignalCounter`: `policyChanges_oneYield_afterSuccessfulAssign`, `policyChanges_oneYield_afterSuccessfulRevoke`, `policyChanges_noYield_onFailedMutation`, `policyChanges_appStateObserverPattern` — prove stream emits exactly one yield per success, zero on failure, and AppState-like observer increments revision counter. Reviewer GO received. S2 remains IMPLEMENTED. | ACCEPTED — closes OPS-003D combined acceptance |
| **CI-001C: sixth `sensitive-export` named check + S2 PASS** | SHA `68f3759da2213441593553e1b8e914ae5e1aff88`. Hosted run **30264550724**, success, 2026-07-27. Full-suite build-and-test: total=967 passed=965 failed=0 skipped=2 (floor=967; +10 `SensitiveScopeEndToEndTests` over OPS-003D.1.2.1). Group: sensitive-export total=122 passed=122 failed=0 ranSuites=7 (floor=122); migration-matrix 37/7; parser-fixtures 78/20; report-receipt-integrity 77/7; architecture-guards ✓. All six checks green. 10 cross-surface E2E tests added (`SensitiveScopeEndToEndTests`): (1) privileged KO denied on all surfaces simultaneously; (2) no identifying leakage — sentinel text/KO-UUID absent from all sections; (3) broken lineage denied on screen and retrieval; (4) cross-workspace denied on retrieval and export; (5) prompt-time mutation revalidation; (6) mutation-service block+restore across all surfaces; (7) file-level inheritance prevents premature KO restore; (8) persona invariance — all 4 templates blocked by same SSA; (9) blocked material absent from sections, manifest, and receipt hashes; (10) nil repo fail-closed on screen and export. S2 moves IMPLEMENTED → PASS. | PASS — closes CI-001C + S2 = PASS |
| OPS-004 WorkProductRun persistence (schema v72) hosted-green | SHA `8678c0cea7f84a928b52191290ddaab810e8095a`. Hosted run **30271215068**, success, 2026-07-27. build-and-test: total=981 passed=979 failed=0 skipped=2 (floor=981; +14 over CI-001C: 4 WorkProductRunMigrationTests + 10 WorkProductRunRepositoryTests). migration-matrix: total=41 passed=41 failed=0 ranSuites=8 (floor=41; +WorkProductRunMigrationTests). sensitive-export: total=122 passed=122 failed=0 ranSuites=7 (floor=122, unchanged). parser-fixtures: 78/20; report-receipt-integrity: 77/7; architecture-guards ✓. All six checks green. Schema v72: 4 tables (`work_product_runs` / `work_product_sections` / `work_product_claim_occurrences` / `work_product_manifests`); all CASCADE from run delete; no modification to canonical tables. `WorkProductRunRepository` (actor): SAVEPOINT-atomic `save()`/`reopen()`/`runs(inWorkspace:)`/`delete()`/`manifest(forRun:)`; full `CitationRecord` + `ExportManifest` JSON round-trip; canonical isolation proven (delete never touches claims/evidence/entities/events). `WorkProductValidationRecord` for future hash-revalidation path. Acceptance scope: persistent immutable runs, full section+claim+manifest round-trip, workspace isolation, fail-closed reopen, CASCADE limited to saved-run records, canonical ledger untouched. OPS-004.1 fix: two `SensitiveScopeMigrationTests` were calling `migrate(db)` without `through: 71`, relying on latestVersion==71; OPS-004 bumped latestVersion to 72, exposing the latent unpin — fix pinned both to `through: 71`. baselineCommit updated from `68f3759` to `8678c0c`. | PASS — closes OPS-004 |
| **Stage 3 — Persona Job Engine (PJE-001..012) COMPLETE** | Reusable version-pinned workflow engine: definitions/registries/persistent runs/lifecycle/requirements/all 17 executors/evidence+provenance bridge/attachments/generic method boundary/work-product integration/presentation-only terminology/proposal-only automation. Schema v78. All units NORMAL-merged (impl+evidence preserved). Latest hosted-green: PJE-011 run **30560724321** (SHA `2896e13`, all 6 jobs green, floor 1844) then PJE-012 (floor 1851; closes reviewEvidence + `.brokenLineage` rulings; no schema/production change). Accepted-unit SHAs + full capability/kind/persistence/safety/closure audit in **`STAGE3_ACCEPTANCE.md`**. Stage 4 methods, Stage 5 Workbench, Stage 6 persona UI remain later stages. | PASS — closes Stage 3 |
| Branch protection ruleset for `main` | OWNER repo-settings step — now require ALL SIX checks: `build-and-test`, `architecture-guards`, `migration-matrix`, `parser-fixtures`, `report-receipt-integrity`, `sensitive-export`; branch up-to-date; conversations resolved; no force-push/deletion. Sixth check added by CI-001C. | PENDING (owner) |

---

**Note on current CI evidence.** The latest `main` records **781/781 tests green on a real
hosted GitHub run** (see §G) — local counts are no longer the sole gate. Remaining F-series
gates stay below `PASS` until their specific jobs exist (CI-001B) and the branch ruleset is
configured + recorded here.
